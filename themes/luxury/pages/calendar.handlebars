{{!-- Luxury Theme Calendar Page - Royal Appointment Scheduler --}}

<!-- Dramatic Calendar Hero Section -->
<section class="luxury-calendar-hero" data-aos="fade-in">
    <!-- Rich Gradient Background -->
    <div class="luxury-calendar-hero-bg"></div>
    
    <!-- Floating Luxurious Elements -->
    <div class="luxury-hero-floating-elements">
        <div class="luxury-floating-element luxury-element-calendar">📅</div>
        <div class="luxury-floating-element luxury-element-clock">🕐</div>
        <div class="luxury-floating-element luxury-element-crown">👑</div>
        <div class="luxury-floating-element luxury-element-scroll">📜</div>
        <div class="luxury-floating-element luxury-element-gem">💎</div>
        <div class="luxury-floating-element luxury-element-quill">🪶</div>
    </div>
    
    <!-- Royal Overlay for Text Contrast -->
    <div class="luxury-hero-overlay"></div>
    
    <div class="max-w-6xl mx-auto text-center px-4 sm:px-6 lg:px-8 relative z-20">
        <!-- Imperial Crown Icon -->
        <div class="luxury-calendar-hero-icon" data-aos="fade-down" data-aos-delay="200">
            <i class="fas fa-calendar-alt"></i>
        </div>
        
        <!-- Majestic Title -->
        <h1 class="luxury-calendar-hero-title" data-aos="fade-up" data-aos-delay="400">
            {{#if calendar_content.page_title}}{{calendar_content.page_title}}{{else}}Royal Appointment Calendar{{/if}}
        </h1>
        
        <!-- Elegant Subtitle -->
        {{#if calendar_content.page_subtitle}}
        <p class="luxury-calendar-hero-subtitle" data-aos="fade-up" data-aos-delay="600">
            {{calendar_content.page_subtitle}}
        </p>
        {{else}}
        <p class="luxury-calendar-hero-subtitle" data-aos="fade-up" data-aos-delay="600">
            View my current location and availability. Check where I am traveling and when I am available for exclusive appointments.
        </p>
        {{/if}}
        
        <!-- Royal Divider with Crown -->
        <div class="luxury-calendar-hero-divider" data-aos="fade-up" data-aos-delay="800">
            <div class="luxury-divider-line-left"></div>
            <div class="luxury-divider-crown">
                <i class="fas fa-crown"></i>
            </div>
            <div class="luxury-divider-line-right"></div>
        </div>
    </div>
    
    <!-- Floating Sparkles -->
    <div class="luxury-hero-sparkles">
        <div class="luxury-sparkle luxury-sparkle-1">✨</div>
        <div class="luxury-sparkle luxury-sparkle-2">✨</div>
        <div class="luxury-sparkle luxury-sparkle-3">✨</div>
        <div class="luxury-sparkle luxury-sparkle-4">✨</div>
    </div>
</section>

<!-- Main Calendar Palace Section -->
<section class="luxury-calendar-palace">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        
        <!-- Calendar Navigation -->
        <div class="luxury-calendar-navigation" data-aos="fade-up">
            <button id="prev-month" class="luxury-nav-btn luxury-nav-prev">
                <i class="fas fa-chevron-left"></i>
                <span>Previous</span>
            </button>
            
            <h2 id="month-title" class="luxury-month-title"></h2>
            
            <button id="next-month" class="luxury-nav-btn luxury-nav-next">
                <span>Next</span>
                <i class="fas fa-chevron-right"></i>
            </button>
        </div>

        <!-- Royal Calendar Chamber -->
        <div class="luxury-calendar-chamber" data-aos="fade-up" data-aos-delay="200">
            <!-- Calendar Days Header -->
            <div class="luxury-calendar-header-row">
                <div class="luxury-calendar-day-header">Sun</div>
                <div class="luxury-calendar-day-header">Mon</div>
                <div class="luxury-calendar-day-header">Tue</div>
                <div class="luxury-calendar-day-header">Wed</div>
                <div class="luxury-calendar-day-header">Thu</div>
                <div class="luxury-calendar-day-header">Fri</div>
                <div class="luxury-calendar-day-header">Sat</div>
            </div>
            
            <!-- Calendar Grid -->
            <div id="calendar-grid" class="luxury-calendar-grid">
                <div class="luxury-calendar-loading">
                    <div class="luxury-loading-crown">
                        <i class="fas fa-crown"></i>
                    </div>
                    <p class="luxury-loading-text">Loading Royal Schedule...</p>
                </div>
            </div>
        </div>

        <!-- Royal Legend -->
        <div class="luxury-calendar-legend" data-aos="fade-up" data-aos-delay="400">
            <div class="luxury-legend-item">
                <div class="luxury-legend-indicator luxury-available"></div>
                <span class="luxury-legend-label">Available</span>
            </div>
            <div class="luxury-legend-item">
                <div class="luxury-legend-indicator luxury-travel"></div>
                <span class="luxury-legend-label">Travel</span>
            </div>
            <div class="luxury-legend-item">
                <div class="luxury-legend-indicator luxury-vacation"></div>
                <span class="luxury-legend-label">Vacation</span>
            </div>
            <div class="luxury-legend-item">
                <div class="luxury-legend-indicator luxury-unavailable"></div>
                <span class="luxury-legend-label">Unavailable</span>
            </div>
        </div>

        <!-- Royal CTA Palace -->
        <div class="luxury-calendar-cta" data-aos="fade-up" data-aos-delay="600">
            <div class="luxury-cta-ornament">
                <i class="fas fa-gem"></i>
            </div>
            <h3 class="luxury-cta-title">
                {{#if calendar_content.cta_title}}{{calendar_content.cta_title}}{{else}}Schedule an Imperial Appointment{{/if}}
            </h3>
            <p class="luxury-cta-description">
                {{#if calendar_content.cta_text}}{{calendar_content.cta_text}}{{else}}Contact me to schedule an exclusive appointment when I am in your area.{{/if}}
            </p>
            <div class="luxury-cta-buttons">
                <a href="/{{modelSlug}}/contact{{previewParam}}" class="luxury-btn luxury-btn-primary">
                    <i class="fas fa-scroll mr-2"></i>
                    <span>{{#if calendar_content.cta_button_text}}{{calendar_content.cta_button_text}}{{else}}Contact Me{{/if}}</span>
                    <div class="luxury-btn-glow"></div>
                </a>
                <a href="/{{modelSlug}}/rates{{previewParam}}" class="luxury-btn luxury-btn-secondary">
                    <i class="fas fa-crown mr-2"></i>
                    <span>View Services</span>
                    <div class="luxury-btn-shine"></div>
                </a>
            </div>
        </div>
    </div>
</section>

{{!-- Calendar JavaScript --}}
<script>
(function() {
    const slug = {{{json modelSlug}}};
    let currentYear = new Date().getFullYear();
    let currentMonth = new Date().getMonth() + 1;
    
    const calendarGrid = document.getElementById('calendar-grid');
    const monthTitle = document.getElementById('month-title');
    const prevButton = document.getElementById('prev-month');
    const nextButton = document.getElementById('next-month');
    
    async function loadCalendar(year = currentYear, month = currentMonth) {
        try {
            const response = await fetch(`/api/model-calendar/${slug}?year=${year}&month=${month}`);
            const data = await response.json();
            
            if (!data.success) {
                throw new Error(data.error || 'Failed to load calendar');
            }
            
            const calendar = data.data.calendar;
            currentYear = year;
            currentMonth = month;
            
            // Update month title
            monthTitle.textContent = `${calendar.monthName} ${calendar.year}`;
            
            // Generate calendar grid
            let gridHtml = '';
            
            calendar.weeks.forEach((week, weekIndex) => {
                gridHtml += `<div class="grid grid-cols-7 border-t border-white border-opacity-10 relative" style="min-height: ${96 + (week.spans.length * 28)}px;">`;
                
                // Day cells
                week.days.forEach((day, dayIndex) => {
                    const isToday = day === new Date().getDate() && 
                                  currentMonth === (new Date().getMonth() + 1) && 
                                  currentYear === new Date().getFullYear();
                    
                    gridHtml += `
                        <div class="p-3 border-r border-white border-opacity-10 ${day === 0 ? 'bg-black bg-opacity-20' : ''} relative min-h-24">
                            ${day > 0 ? `
                                <div class="text-sm font-light mb-2 ${isToday ? 'text-yellow-400' : 'text-slate-200'} relative z-10 tracking-wide">
                                    ${day}
                                </div>
                            ` : ''}
                        </div>
                    `;
                });
                
                // Add availability spans positioned absolutely over the week
                week.spans.forEach((span, spanIndex) => {
                    gridHtml += `
                        <div class="absolute text-xs rounded px-2 py-1 z-10 flex items-center justify-center font-medium shadow-2xl text-white"
                             style="background-color: ${span.color}; 
                                    left: ${span.start_pos * 14.285714}%; 
                                    width: ${span.width * 14.285714}%;
                                    top: ${36 + (spanIndex * 28)}px; 
                                    height: 24px;
                                    margin-left: 1px;
                                    margin-right: 1px;
                                    backdrop-filter: blur(4px);">
                            <span class="truncate text-xs font-light tracking-wide">
                                ${span.location}
                                ${span.status === 'vacation' ? ' - Vacation' : ''}
                            </span>
                        </div>
                    `;
                });
                
                gridHtml += '</div>';
            });
            
            calendarGrid.innerHTML = gridHtml;
            
        } catch (error) {
            console.error('Error loading calendar:', error);
            calendarGrid.innerHTML = `
                <div class="text-center py-20 text-red-300">
                    <div class="text-6xl mb-8 opacity-50">⚠️</div>
                    <h3 class="text-2xl font-light mb-4">Calendar Unavailable</h3>
                    <p class="text-lg">Please contact me directly for scheduling.</p>
                </div>
            `;
        }
    }
    
    // Navigation event listeners
    prevButton.addEventListener('click', () => {
        if (currentMonth === 1) {
            currentMonth = 12;
            currentYear--;
        } else {
            currentMonth--;
        }
        loadCalendar(currentYear, currentMonth);
    });
    
    nextButton.addEventListener('click', () => {
        if (currentMonth === 12) {
            currentMonth = 1;
            currentYear++;
        } else {
            currentMonth++;
        }
        loadCalendar(currentYear, currentMonth);
    });
    
    // Load initial calendar
    loadCalendar();
})();
</script>

<!-- Luxury Calendar Styling -->
<style>
/* CSS Variables for Luxury Calendar - canonical tokens only */
:root {
    --luxury-marble-white: var(--theme-bg-secondary);
    --luxury-marble-cream: var(--theme-bg-tertiary);
    --luxury-pearl: var(--theme-text-primary);
    --luxury-gold: var(--theme-accent);
    --luxury-rich-gold: var(--theme-accent-hover);
    --luxury-rose-gold: var(--theme-accent-light);
    --luxury-deep-burgundy: var(--theme-text);
    --luxury-royal-purple: var(--theme-secondary, var(--theme-accent-hover));
    --luxury-obsidian: var(--theme-bg-primary);
    --luxury-text: var(--theme-text);
}

/* Dramatic Calendar Hero Section */
.luxury-calendar-hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 8rem 0 6rem;
    overflow: hidden;
}

.luxury-calendar-hero-bg {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, var(--theme-bg-primary) 0%, var(--theme-secondary, var(--theme-accent-hover)) 25%, var(--theme-error) 50%, var(--theme-accent) 75%, var(--theme-secondary, var(--theme-accent-hover)) 100%);
    opacity: 0.95;
}

.luxury-hero-overlay {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: radial-gradient(ellipse at center, var(--bg-dark) 0%, var(--bg-dark) 100%); filter: opacity(0.1);
    z-index: 5;
}

/* Floating Elements */
.luxury-hero-floating-elements {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 10;
}

.luxury-floating-element {
    position: absolute;
    font-size: 2.5rem;
    opacity: 0.7;
    animation: luxury-hero-float 8s ease-in-out infinite;
}

.luxury-element-calendar {
    top: 15%;
    left: 8%;
    animation-delay: 0s;
    font-size: 3rem;
}

.luxury-element-clock {
    top: 25%;
    right: 12%;
    animation-delay: 1.5s;
    font-size: 2.2rem;
}

.luxury-element-crown {
    top: 45%;
    left: 5%;
    animation-delay: 3s;
    font-size: 2.8rem;
}

.luxury-element-scroll {
    top: 65%;
    right: 8%;
    animation-delay: 4.5s;
    font-size: 2.6rem;
}

.luxury-element-gem {
    bottom: 20%;
    left: 15%;
    animation-delay: 6s;
    font-size: 2rem;
}

.luxury-element-quill {
    bottom: 35%;
    right: 20%;
    animation-delay: 7.5s;
    font-size: 2.4rem;
}

@keyframes luxury-hero-float {
    0%, 100% { 
        transform: translateY(0px) rotate(0deg) scale(1);
        opacity: 0.7;
    }
    25% { 
        transform: translateY(-20px) rotate(5deg) scale(1.1);
        opacity: 0.9;
    }
    50% { 
        transform: translateY(-10px) rotate(-3deg) scale(1.05);
        opacity: 1;
    }
    75% { 
        transform: translateY(-25px) rotate(8deg) scale(1.15);
        opacity: 0.8;
    }
}

/* Hero Content */
.luxury-calendar-hero-icon {
    font-size: 5rem;
    color: var(--theme-accent);
    margin-bottom: 2.5rem;
    filter: drop-shadow(0 0 30px var(--card-shadow));
    animation: luxury-hero-crown-pulse 4s infinite ease-in-out;
}

@keyframes luxury-hero-crown-pulse {
    0%, 100% { 
        transform: scale(1);
        filter: drop-shadow(0 0 30px var(--card-shadow));
    }
    50% { 
        transform: scale(1.15);
        filter: drop-shadow(0 0 50px var(--shadow-hover));
    }
}

.luxury-calendar-hero-title {
    font-size: 5.5rem;
    font-weight: 900;
    text-align: center;
    margin-bottom: 2rem;
    color: var(--theme-text-primary);
    text-shadow: 
        0 0 20px var(--theme-accent),
        0 4px 15px var(--theme-bg-primary);
        0 2px 30px var(--card-shadow);
    letter-spacing: 3px;
    font-family: 'Playfair Display', serif;
    line-height: 1.1;
    background: linear-gradient(45deg, var(--theme-text-primary) 0%, var(--theme-accent) 50%, var(--theme-text-primary) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.luxury-calendar-hero-subtitle {
    font-size: 1.6rem;
    color: var(--theme-text-primary); filter: opacity(0.95);
    text-align: center;
    margin-bottom: 3rem;
    font-weight: 400;
    letter-spacing: 1px;
    max-width: 700px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.7;
    text-shadow: 0 2px 10px var(--bg-dark); filter: opacity(0.3);
    font-style: italic;
}

.luxury-calendar-hero-divider {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 2rem;
    margin-top: 3rem;
}

.luxury-divider-line-left,
.luxury-divider-line-right {
    width: 120px;
    height: 3px;
    background: linear-gradient(90deg, transparent, var(--theme-accent), var(--theme-text-primary)); filter: opacity(0.8);
    border-radius: 2px;
}

.luxury-divider-crown {
    font-size: 2rem;
    color: var(--theme-accent);
    animation: luxury-divider-crown-glow 3s infinite ease-in-out;
    filter: drop-shadow(0 0 15px var(--card-shadow));
}

@keyframes luxury-divider-crown-glow {
    0%, 100% { 
        opacity: 0.8;
        transform: scale(1) rotate(0deg);
    }
    50% { 
        opacity: 1;
        transform: scale(1.2) rotate(10deg);
    }
}

/* Floating Sparkles */
.luxury-hero-sparkles {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 15;
}

.luxury-sparkle {
    position: absolute;
    font-size: 1.5rem;
    opacity: 0.8;
    animation: luxury-sparkle-twinkle 4s ease-in-out infinite;
}

.luxury-sparkle-1 {
    top: 20%;
    left: 25%;
    animation-delay: 0s;
}

.luxury-sparkle-2 {
    top: 35%;
    right: 30%;
    animation-delay: 1s;
}

.luxury-sparkle-3 {
    bottom: 25%;
    left: 35%;
    animation-delay: 2s;
}

.luxury-sparkle-4 {
    bottom: 40%;
    right: 25%;
    animation-delay: 3s;
}

@keyframes luxury-sparkle-twinkle {
    0%, 100% { 
        opacity: 0.3;
        transform: scale(0.5) rotate(0deg);
    }
    50% { 
        opacity: 1;
        transform: scale(1.2) rotate(180deg);
    }
}

/* Calendar Palace Section */
.luxury-calendar-palace {
    padding: 6rem 0;
    background: linear-gradient(180deg, var(--luxury-pearl) 0%, var(--luxury-marble-cream) 100%);
}

/* Calendar Navigation */
.luxury-calendar-navigation {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 4rem;
    padding: 0 2rem;
}

.luxury-nav-btn {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2rem;
    background: linear-gradient(135deg, var(--luxury-gold) 0%, var(--luxury-rich-gold) 100%);
    color: var(--luxury-obsidian);
    border: none;
    border-radius: 50px;
    font-weight: 600;
    font-size: 1.1rem;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 8px 25px var(--card-shadow);
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.luxury-nav-btn:hover {
    transform: translateY(-3px) scale(1.05);
    box-shadow: 0 12px 35px var(--shadow-hover);
}

.luxury-month-title {
    font-size: 3rem;
    font-weight: 700;
    background: linear-gradient(45deg, var(--luxury-rich-gold) 0%, var(--luxury-gold) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: 0 2px 8px var(--card-shadow);
    font-family: 'Playfair Display', serif;
    letter-spacing: 1px;
}

/* Royal Calendar Chamber */
.luxury-calendar-chamber {
    background: linear-gradient(145deg, var(--theme-text-primary), var(--theme-bg-secondary)); filter: opacity(0.9);
    border-radius: 25px;
    box-shadow: 0 25px 60px var(--theme-bg-primary); filter: opacity(0.1);
    border: 2px solid var(--theme-accent); filter: opacity(0.2);
    overflow: hidden;
    margin-bottom: 4rem;
}

.luxury-calendar-header-row {
    display: grid;
    grid-template-columns: repeat(7, 1fr);
    background: linear-gradient(135deg, var(--luxury-gold) 0%, var(--luxury-rich-gold) 100%);
    border-bottom: 2px solid var(--theme-accent); filter: opacity(0.3);
}

.luxury-calendar-day-header {
    padding: 1.5rem;
    text-align: center;
    font-weight: 700;
    color: var(--luxury-obsidian);
    text-transform: uppercase;
    letter-spacing: 1px;
    border-right: 1px solid var(--theme-bg-primary); filter: opacity(0.1);
    font-size: 0.95rem;
}

.luxury-calendar-day-header:last-child {
    border-right: none;
}

.luxury-calendar-grid {
    padding: 2rem;
    min-height: 400px;
}

/* Calendar Loading State */
.luxury-calendar-loading {
    text-align: center;
    padding: 4rem 0;
}

.luxury-loading-crown {
    font-size: 3rem;
    color: var(--luxury-gold);
    margin-bottom: 1.5rem;
    animation: luxury-loading-spin 2s linear infinite;
}

@keyframes luxury-loading-spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

.luxury-loading-text {
    font-size: 1.2rem;
    color: var(--luxury-deep-burgundy);
    font-weight: 500;
    letter-spacing: 0.5px;
}

/* Calendar Day Cells */
.calendar-day {
    aspect-ratio: 1;
    border: 1px solid var(--accent); filter: opacity(0.2);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 0.75rem;
    transition: all 0.3s ease;
    cursor: pointer;
    background: var(--text-light); filter: opacity(0.7);
}

.calendar-day:hover {
    background: var(--accent); filter: opacity(0.1);
    transform: scale(1.02);
    box-shadow: 0 4px 15px var(--card-shadow);
}

.calendar-day-number {
    font-weight: 600;
    font-size: 1.1rem;
    color: var(--luxury-obsidian);
}

.calendar-day-status {
    font-size: 0.75rem;
    padding: 0.25rem 0.5rem;
    border-radius: 12px;
    text-align: center;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

/* Royal Legend */
.luxury-calendar-legend {
    display: flex;
    justify-content: center;
    gap: 3rem;
    margin-bottom: 4rem;
    flex-wrap: wrap;
}

.luxury-legend-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 1.5rem;
    background: var(--text-light); filter: opacity(0.8);
    border-radius: 50px;
    border: 1px solid var(--accent); filter: opacity(0.3);
    box-shadow: 0 4px 15px var(--card-shadow);
}

.luxury-legend-indicator {
    width: 16px;
    height: 16px;
    border-radius: 4px;
    box-shadow: 0 2px 8px var(--bg-dark); filter: opacity(0.2);
}

.luxury-available { background: linear-gradient(135deg, var(--success), var(--success-dark)); }
.luxury-travel { background: linear-gradient(135deg, var(--theme-accent), var(--theme-accent-hover)); }
.luxury-vacation { background: linear-gradient(135deg, var(--theme-warning), var(--theme-accent-light)); }
.luxury-unavailable { background: linear-gradient(135deg, var(--theme-border-primary), var(--theme-border-light)); }

.luxury-legend-label {
    font-weight: 600;
    color: var(--luxury-obsidian);
    font-size: 1rem;
    letter-spacing: 0.5px;
}

/* Royal CTA Palace */
.luxury-calendar-cta {
    background: linear-gradient(145deg, var(--theme-accent) 0%, var(--theme-accent-light) 100%); filter: opacity(0.1);
    border: 2px solid var(--theme-accent); filter: opacity(0.3);
    border-radius: 30px;
    padding: 4rem 3rem;
    text-align: center;
    position: relative;
    overflow: hidden;
}

.luxury-cta-ornament {
    font-size: 3rem;
    color: var(--luxury-gold);
    margin-bottom: 2rem;
    animation: luxury-cta-ornament-glow 4s infinite ease-in-out;
}

@keyframes luxury-cta-ornament-glow {
    0%, 100% { opacity: 0.8; transform: scale(1); }
    50% { opacity: 1; transform: scale(1.1); }
}

.luxury-cta-title {
    font-size: 2.5rem;
    font-weight: 700;
    background: linear-gradient(45deg, var(--luxury-rich-gold) 0%, var(--luxury-gold) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 1.5rem;
    font-family: 'Playfair Display', serif;
    letter-spacing: 1px;
}

.luxury-cta-description {
    font-size: 1.2rem;
    color: var(--luxury-deep-burgundy);
    margin-bottom: 3rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    line-height: 1.7;
    font-weight: 400;
}

.luxury-cta-buttons {
    display: flex;
    justify-content: center;
    gap: 2rem;
    flex-wrap: wrap;
}

.luxury-btn {
    position: relative;
    display: inline-flex;
    align-items: center;
    padding: 1rem 2.5rem;
    border-radius: 50px;
    font-weight: 600;
    font-size: 1.1rem;
    text-decoration: none;
    transition: all 0.3s ease;
    cursor: pointer;
    border: none;
    overflow: hidden;
    text-transform: uppercase;
    letter-spacing: 0.5px;
}

.luxury-btn-primary {
    background: linear-gradient(135deg, var(--luxury-gold) 0%, var(--luxury-rich-gold) 100%);
    color: var(--luxury-obsidian);
    box-shadow: 0 8px 25px var(--card-shadow);
}

.luxury-btn-primary:hover {
    transform: translateY(-3px) scale(1.05);
    box-shadow: 0 15px 40px var(--shadow-hover);
}

.luxury-btn-secondary {
    background: transparent;
    color: var(--luxury-gold);
    border: 2px solid var(--luxury-gold);
    box-shadow: 0 8px 25px var(--card-shadow);
}

.luxury-btn-secondary:hover {
    background: var(--luxury-gold);
    color: var(--luxury-obsidian);
    transform: translateY(-3px) scale(1.05);
    box-shadow: 0 15px 40px var(--card-shadow);
}

.luxury-btn-glow,
.luxury-btn-shine {
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, var(--text-light), transparent); filter: opacity(0.3);
    transition: left 0.6s;
}

.luxury-btn:hover .luxury-btn-glow,
.luxury-btn:hover .luxury-btn-shine {
    left: 100%;
}

/* --- Remove overlays/dimming on Calendar page --- */
.luxury-hero-overlay { display: none !important; background: transparent !important; }
.luxury-calendar-hero-bg { opacity: 1 !important; filter: none !important; }
/* Disable any pseudo-overlay shine on buttons */
.luxury-btn-glow, .luxury-btn-shine { display: none !important; }

/* Responsive Design */
@media (max-width: 768px) {
    .luxury-calendar-hero {
        min-height: 70vh;
        padding: 6rem 0 4rem;
    }
    
    .luxury-calendar-hero-title {
        font-size: 3.5rem;
        letter-spacing: 2px;
    }
    
    .luxury-calendar-hero-subtitle {
        font-size: 1.2rem;
        padding: 0 1rem;
    }
    
    .luxury-calendar-hero-icon {
        font-size: 4rem;
        margin-bottom: 2rem;
    }
    
    .luxury-floating-element {
        font-size: 2rem;
    }
    
    .luxury-element-calendar {
        font-size: 2.5rem;
    }
    
    .luxury-hero-sparkles .luxury-sparkle {
        font-size: 1.2rem;
    }
    
    .luxury-month-title {
        font-size: 2rem;
    }
    
    .luxury-calendar-navigation {
        flex-direction: column;
        gap: 1.5rem;
        padding: 0 1rem;
    }
    
    .luxury-nav-btn {
        padding: 0.75rem 1.5rem;
        font-size: 1rem;
    }
    
    .luxury-calendar-legend {
        gap: 1.5rem;
        padding: 0 1rem;
    }
    
    .luxury-cta-title {
        font-size: 2rem;
    }
    
    .luxury-cta-buttons {
        flex-direction: column;
        align-items: center;
    }
    
    .luxury-btn {
        width: 100%;
        max-width: 280px;
        justify-content: center;
    }
}

@media (max-width: 480px) {
    .luxury-calendar-hero {
        min-height: 60vh;
        padding: 4rem 0 3rem;
    }
    
    .luxury-calendar-hero-title {
        font-size: 2.8rem;
        letter-spacing: 1px;
    }
    
    .luxury-calendar-hero-subtitle {
        font-size: 1.1rem;
        padding: 0 1.5rem;
    }
    
    .luxury-calendar-hero-icon {
        font-size: 3.5rem;
        margin-bottom: 1.5rem;
    }
    
    .luxury-floating-element {
        font-size: 1.8rem;
    }
    
    .luxury-element-calendar {
        font-size: 2.2rem;
    }
    
    .luxury-divider-line-left,
    .luxury-divider-line-right {
        width: 80px;
    }
    
    .luxury-calendar-legend {
        flex-direction: column;
        align-items: center;
        gap: 1rem;
    }
    
    .luxury-calendar-cta {
        padding: 3rem 1.5rem;
        margin: 0 1rem;
    }
}
</style>
