<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{pageTitle}} - {{siteName}}</title>
    <meta name="description" content="{{metaDescription}}">
    <meta name="og:title" content="{{pageTitle}} - {{siteName}}">
    <meta name="og:description" content="{{metaDescription}}">
    <meta name="og:type" content="website">
    {{#if ogImage}}
    <meta name="og:image" content="{{ogImage}}">
    {{/if}}
    
    <!-- Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <!-- Google Fonts - Luxury Typography -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700;800;900&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- AOS Animation Library -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    
    <style>
        :root {
            --luxury-primary: {{theme.colors.primary}};
            --luxury-secondary: {{theme.colors.secondary}};
            --luxury-accent: {{theme.colors.accent}};
            --luxury-text: {{theme.colors.text}};
            --luxury-background: {{theme.colors.background}};
        }
        
        /* Luxury Typography */
        .luxury-heading {
            font-family: 'Playfair Display', serif;
            font-weight: 700;
            letter-spacing: -0.025em;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--luxury-background);
            color: var(--luxury-text);
        }
        
        /* Luxury Card Styling */
        .luxury-card {
            background: linear-gradient(145deg, #ffffff 0%, #fefefe 100%);
            border-radius: 20px;
            border: 1px solid rgba(0,0,0,0.05);
            box-shadow: 0 20px 40px rgba(0,0,0,0.1), 0 1px 3px rgba(0,0,0,0.05);
            transition: all 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }
        
        .luxury-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--luxury-primary), var(--luxury-accent));
        }
        
        .luxury-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 32px 64px rgba(0,0,0,0.15), 0 1px 3px rgba(0,0,0,0.05);
        }
        
        /* Luxury Button Styling */
        .luxury-btn {
            background: linear-gradient(135deg, var(--luxury-primary) 0%, var(--luxury-secondary) 100%);
            color: white;
            padding: 16px 32px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 16px;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
            box-shadow: 0 8px 32px rgba(0,0,0,0.2);
            border: none;
            cursor: pointer;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            position: relative;
            overflow: hidden;
        }
        
        .luxury-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: left 0.6s;
        }
        
        .luxury-btn:hover::before {
            left: 100%;
        }
        
        .luxury-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 48px rgba(0,0,0,0.25);
        }
        
        .luxury-btn-outline {
            background: transparent;
            color: var(--luxury-primary);
            border: 2px solid var(--luxury-primary);
            padding: 14px 30px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 16px;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .luxury-btn-outline:hover {
            background: var(--luxury-primary);
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 8px 32px rgba(0,0,0,0.2);
        }
        
        /* Luxury Navigation */
        .luxury-nav {
            background: rgba(255,255,255,0.95);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(0,0,0,0.1);
            box-shadow: 0 4px 24px rgba(0,0,0,0.1);
        }
        
        .luxury-nav .nav-link {
            font-weight: 500;
            color: var(--luxury-text);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 14px;
            transition: all 0.3s ease;
            position: relative;
        }
        
        .luxury-nav .nav-link::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 50%;
            transform: translateX(-50%);
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, var(--luxury-primary), var(--luxury-accent));
            transition: width 0.3s ease;
        }
        
        .luxury-nav .nav-link:hover::after,
        .luxury-nav .nav-link.active::after {
            width: 100%;
        }
        
        .luxury-nav .nav-link:hover,
        .luxury-nav .nav-link.active {
            color: var(--luxury-primary);
        }
        
        /* Luxury Brand */
        .luxury-brand {
            font-family: 'Playfair Display', serif;
            font-weight: 800;
            font-size: 28px;
            color: var(--luxury-primary);
            text-decoration: none;
            letter-spacing: -0.5px;
        }
        
        /* Hero Section Styling */
        .luxury-hero {
            background: linear-gradient(135deg, var(--luxury-primary) 0%, var(--luxury-secondary) 50%, var(--luxury-accent) 100%);
            position: relative;
            overflow: hidden;
        }
        
        .luxury-hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="luxury-pattern" x="0" y="0" width="20" height="20" patternUnits="userSpaceOnUse"><circle cx="10" cy="10" r="1" fill="rgba(255,255,255,0.1)"/></pattern></defs><rect width="100" height="100" fill="url(%23luxury-pattern)"/></svg>');
            opacity: 0.3;
        }
        
        /* Animations */
        @keyframes luxuryFloat {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(2deg); }
        }
        
        .luxury-float {
            animation: luxuryFloat 6s ease-in-out infinite;
        }
        
        /* Responsive Design */
        @media (max-width: 768px) {
            .luxury-heading {
                font-size: 2.5rem;
            }
            
            .luxury-card {
                border-radius: 15px;
            }
            
            .luxury-btn {
                padding: 12px 24px;
                font-size: 14px;
            }
        }
        
        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }
        
        ::-webkit-scrollbar-track {
            background: #f1f1f1;
        }
        
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(135deg, var(--luxury-primary), var(--luxury-secondary));
            border-radius: 4px;
        }
        
        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(135deg, var(--luxury-secondary), var(--luxury-primary));
        }
    </style>
</head>
<body>
    <!-- Navigation -->
    {{> navigation}}
    
    <!-- Main Content -->
    <main>
        {{{body}}}
    </main>
    
    <!-- Footer -->
    {{> footer}}
    
    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    
    <script>
        // Initialize AOS
        AOS.init({
            duration: 800,
            easing: 'ease-in-out',
            once: true,
            offset: 100
        });
        
        // Luxury floating elements
        document.addEventListener('DOMContentLoaded', function() {
            const floatingElements = document.querySelectorAll('.luxury-float');
            floatingElements.forEach((el, index) => {
                el.style.animationDelay = `${index * 0.5}s`;
            });
        });
        
        // Smooth scroll for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
        
        // Luxury navigation background on scroll
        window.addEventListener('scroll', function() {
            const nav = document.querySelector('.luxury-nav');
            if (window.scrollY > 50) {
                nav.style.background = 'rgba(255,255,255,0.98)';
                nav.style.boxShadow = '0 8px 32px rgba(0,0,0,0.15)';
            } else {
                nav.style.background = 'rgba(255,255,255,0.95)';
                nav.style.boxShadow = '0 4px 24px rgba(0,0,0,0.1)';
            }
        });
    </script>
</body>
</html>