<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{pageTitle}} - {{siteName}}</title>
    <meta name="description" content="{{metaDescription}}">
    <meta name="og:title" content="{{pageTitle}} - {{siteName}}">
    <meta name="og:description" content="{{metaDescription}}">
    <meta name="og:type" content="website">
    {{#if ogImage}}
    <meta name="og:image" content="{{ogImage}}">
    {{/if}}
    
    <!-- Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <!-- Google Fonts - Luxury Typography -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700;800;900&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- AOS Animation Library -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    
    <style>
        :root {
            /* Luxury theme tokens with sensible fallbacks */
            --luxury-bg-start: {{theme.colors.bg_start}}; /* optional theme override */
            --luxury-bg-end: {{theme.colors.bg_end}};
            --luxury-surface: {{theme.colors.surface}};
            --luxury-card: {{theme.colors.card}};
            --luxury-accent: {{#if theme.colors.accent}}{{theme.colors.accent}}{{else}}#E0B84E{{/if}}; /* gold */
            --luxury-text: {{#if theme.colors.text}}{{theme.colors.text}}{{else}}#e9e7f1{{/if}};
            --luxury-muted: {{#if theme.colors.muted}}{{theme.colors.muted}}{{else}}#b9b5c8{{/if}};
            --luxury-background: {{#if theme.colors.background}}{{theme.colors.background}}{{else}}#0b0b15{{/if}};

            /* Back-compat variables (used by some existing rules) */
            --luxury-primary: var(--luxury-accent);
            --luxury-secondary: {{#if theme.colors.secondary}}{{theme.colors.secondary}}{{else}}#b8860b{{/if}};
        }
        
        /* Luxury Typography System */
        .luxury-heading {
            font-family: 'Playfair Display', serif;
            font-weight: 700;
            letter-spacing: -0.025em;
        }
        
        /* Standardized heading sizes */
        h1, .luxury-h1 {
            font-size: 3.75rem; /* 60px */
            line-height: 1.1;
            font-weight: 800;
            letter-spacing: -0.025em;
        }
        
        h2, .luxury-h2 {
            font-size: 2.5rem; /* 40px */
            line-height: 1.2;
            font-weight: 700;
            letter-spacing: -0.025em;
        }
        
        h3, .luxury-h3 {
            font-size: 1.875rem; /* 30px */
            line-height: 1.3;
            font-weight: 600;
            letter-spacing: -0.025em;
        }
        
        /* Body text consistency */
        .luxury-text-lg {
            font-size: 1.125rem; /* 18px */
            line-height: 1.6;
        }
        
        .luxury-text-base {
            font-size: 1rem; /* 16px */
            line-height: 1.6;
        }
        
        /* Text contrast helpers for gradient backgrounds */
        .luxury-text-contrast {
            text-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
        }
        
        .luxury-text-overlay::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.2);
            border-radius: inherit;
            z-index: -1;
        }
        
        /* Responsive typography */
        @media (max-width: 768px) {
            h1, .luxury-h1 { font-size: 2.5rem; }
            h2, .luxury-h2 { font-size: 2rem; }
            h3, .luxury-h3 { font-size: 1.5rem; }
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--luxury-background);
            color: var(--luxury-text);
            padding-top: 80px; /* Height of luxury navigation (h-20 = 80px) */
        }

        /* Global container and vertical rhythm */
        .luxury-container { max-width: 1200px; margin-left: auto; margin-right: auto; padding-left: 1rem; padding-right: 1rem; }
        .luxury-section { padding-top: 6rem; padding-bottom: 6rem; }
        @media (max-width: 768px) { .luxury-section { padding-top: 4rem; padding-bottom: 4rem; } }
        .luxury-space-y > * + * { margin-top: 4rem; }
        
        /* Luxury Card Styling */
        .luxury-card {
            background: #fff;
            border-radius: 20px;
            border: 1px solid rgba(0,0,0,0.05);
            box-shadow: 0 20px 40px rgba(0,0,0,0.1), 0 1px 3px rgba(0,0,0,0.05);
            transition: all 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
            position: relative;
            overflow: hidden;
        }
        
        .luxury-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--luxury-primary), var(--luxury-accent));
        }
        
        .luxury-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 32px 64px rgba(0,0,0,0.15), 0 1px 3px rgba(0,0,0,0.05);
        }
        
        /* Card variant for dark surfaces */
        .luxury-card-dark {
            background: var(--luxury-card, #131327);
            border-radius: 1rem;
            border: 1px solid rgba(224,184,78,0.15);
            box-shadow: 0 10px 30px rgba(0,0,0,0.35);
        }

        /* Luxury Button Styling */
        .luxury-btn {
            background: linear-gradient(135deg, var(--luxury-accent) 0%, color-mix(in srgb, var(--luxury-accent), #000 25%) 100%);
            color: #0f0f1a;
            padding: 16px 32px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 16px;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
            box-shadow: 0 8px 32px rgba(0,0,0,0.2);
            border: none;
            cursor: pointer;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            position: relative;
            overflow: hidden;
        }
        
        .luxury-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: left 0.6s;
        }
        
        .luxury-btn:hover::before {
            left: 100%;
        }
        
        .luxury-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 48px rgba(0,0,0,0.25);
        }
        
        .luxury-btn-outline {
            background: transparent;
            color: var(--luxury-accent);
            border: 2px solid var(--luxury-accent);
            padding: 14px 30px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 16px;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .luxury-btn-outline:hover {
            background: var(--luxury-accent);
            color: #0f0f1a;
            transform: translateY(-2px);
            box-shadow: 0 8px 32px rgba(0,0,0,0.2);
        }

        /* Focus visibility for accessibility */
        .luxury-btn:focus-visible, .nav-link:focus-visible, a:focus-visible { outline: 2px solid var(--luxury-accent); outline-offset: 2px; border-radius: 8px; }
        
        /* Luxury Navigation */
        .luxury-nav {
            background: rgba(15,15,26,0.45);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(224,184,78,0.15);
            box-shadow: 0 4px 24px rgba(0,0,0,0.25);
            transition: all 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
        }
        
        .luxury-nav .nav-link {
            font-weight: 500;
            color: var(--luxury-text);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            font-size: 14px;
            transition: all 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
            position: relative;
            padding: 12px 16px;
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 44px; /* Consistent touch target */
        }
        
        .luxury-nav .nav-link::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 50%;
            transform: translateX(-50%);
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, var(--luxury-accent), #ffd86b);
            transition: width 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
            border-radius: 1px;
        }
        
        .luxury-nav .nav-link:hover::after,
        .luxury-nav .nav-link.active::after {
            width: 80%;
        }
        
        .luxury-nav .nav-link:hover,
        .luxury-nav .nav-link.active {
            color: var(--luxury-accent);
            background: rgba(224, 184, 78, 0.08);
            transform: translateY(-1px);
        }
        
        .luxury-nav .nav-link:focus-visible {
            outline: 2px solid var(--luxury-accent);
            outline-offset: 2px;
            border-radius: 8px;
        }
        
        /* Luxury Brand */
        .luxury-brand {
            font-family: 'Playfair Display', serif;
            font-weight: 800;
            font-size: 28px;
            color: var(--luxury-primary);
            text-decoration: none;
            letter-spacing: -0.5px;
        }
        
        /* Hero Section Styling */
        .luxury-hero {
            background: radial-gradient(1200px 600px at 50% -10%, rgba(0,0,0,0.25), transparent 60%),
                        linear-gradient(135deg, var(--luxury-bg-start, #2a0f3a) 0%, var(--luxury-bg-end, #7a1a3d) 100%);
            position: relative;
            overflow: hidden;
        }
        
        .luxury-hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><defs><pattern id="luxury-pattern" x="0" y="0" width="20" height="20" patternUnits="userSpaceOnUse"><circle cx="10" cy="10" r="1" fill="rgba(255,255,255,0.1)"/></pattern></defs><rect width="100" height="100" fill="url(%23luxury-pattern)"/></svg>');
            opacity: 0.3;
        }
        
        /* Animations */
        @keyframes luxuryFloat {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(2deg); }
        }
        
        .luxury-float {
            animation: luxuryFloat 6s ease-in-out infinite;
        }
        
        /* Responsive Design */
        @media (max-width: 768px) {
            .luxury-heading {
                font-size: 2.5rem;
            }
            
            .luxury-card {
                border-radius: 15px;
            }
            
            .luxury-btn {
                padding: 12px 24px;
                font-size: 14px;
            }
        }
        
        /* Custom Scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
        }
        
        ::-webkit-scrollbar-track {
            background: #f1f1f1;
        }
        
        ::-webkit-scrollbar-thumb {
            background: linear-gradient(135deg, var(--luxury-primary), var(--luxury-secondary));
            border-radius: 4px;
        }
        
        ::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(135deg, var(--luxury-secondary), var(--luxury-primary));
        }

.luxury-gallery-section { background: var(--luxury-surface); }
.luxury-gallery-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 1rem; }
.luxury-gallery-item { overflow: hidden; border-radius: 0.75rem; }
.luxury-gallery-item img { transition: transform 0.3s ease; }
.luxury-gallery-item:hover img { transform: scale(1.05); }
.luxury-gallery-placeholder { background: var(--luxury-card); color: var(--luxury-muted); }

.luxury-travel-section { background: var(--luxury-surface); }
.luxury-travel-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; }
.luxury-travel-card { text-align: center; }
.luxury-available-badge { background: var(--luxury-accent); color: #fff; padding: 0.5rem 1rem; border-radius: 9999px; }
.luxury-travel-notes { font-style: italic; color: var(--luxury-muted); }

.luxury-testimonials-section { background: var(--luxury-background); }
.luxury-testimonials-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 2rem; }
.luxury-stars { display: flex; justify-content: center; gap: 0.25rem; }
.luxury-testimonial-text { font-style: italic; }
.luxury-testimonial-author { color: var(--luxury-accent); }

/* Responsive Media Queries */
@media (max-width: 1024px) {
  .luxury-gallery-grid { grid-template-columns: repeat(3, 1fr); }
  .luxury-travel-grid, .luxury-testimonials-grid { grid-template-columns: repeat(2, 1fr); }
}
@media (max-width: 768px) {
  .luxury-gallery-grid { grid-template-columns: repeat(2, 1fr); }
  .luxury-travel-grid, .luxury-testimonials-grid { grid-template-columns: 1fr; }
}
    </style>
</head>
<body>
    <!-- Navigation -->
    {{> navigation}}
    
    <!-- Theme Preview Banner -->
    {{#if theme.isPreview}}
    <div class="bg-gradient-to-r from-amber-500 to-yellow-600 text-white py-2 px-4 text-center text-sm font-semibold">
        <i class="fas fa-eye me-2"></i>You are previewing the <strong>{{theme.previewThemeName}}</strong> theme. This is not your active theme.
    </div>
    {{/if}}
    
    <!-- Main Content -->
    <main>
        {{{body}}}
    </main>
    
    <!-- Footer -->
    {{> footer}}
    
    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    
    <script>
        // Initialize AOS
        AOS.init({
            duration: 800,
            easing: 'ease-in-out',
            once: true,
            offset: 100
        });
        
        // Luxury floating elements
        document.addEventListener('DOMContentLoaded', function() {
            const floatingElements = document.querySelectorAll('.luxury-float');
            floatingElements.forEach((el, index) => {
                el.style.animationDelay = `${index * 0.5}s`;
            });
        });
        
        // Smooth scroll for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
        
        // Luxury navigation background on scroll
        window.addEventListener('scroll', function() {
            const nav = document.querySelector('.luxury-nav');
            if (window.scrollY > 50) {
                nav.style.background = 'rgba(255,255,255,0.98)';
                nav.style.boxShadow = '0 8px 32px rgba(0,0,0,0.15)';
            } else {
                nav.style.background = 'rgba(255,255,255,0.95)';
                nav.style.boxShadow = '0 4px 24px rgba(0,0,0,0.1)';
            }
        });
    </script>
</body>
</html>