<div class="sidebar">
  <!-- Sidebar Header -->
  <div class="sidebar-header">
    <a href="/admin" class="sidebar-brand">
      <i class="fas fa-user-circle"></i>
      MuseNest
    </a>
  </div>

  <!-- Sidebar Navigation (Model Admin) -->
  <nav class="sidebar-nav">
    <!-- Overview -->
    <div class="nav-section">
      <div class="nav-section-title">Overview</div>
      <a href="/admin" class="nav-link {{#if (eq currentPage 'model-admin')}}active{{/if}}">
        <i class="fas fa-tachometer-alt"></i>
        Dashboard
      </a>
    </div>

    <!-- Manage Site -->
    <div class="nav-section">
      <div class="nav-section-title">Manage Site</div>
      <a href="/admin/{{#if model.slug}}{{model.slug}}{{else}}modelexample{{/if}}/content" class="nav-link {{#if (eq currentPage 'model-content')}}active{{/if}}">
        <i class="fas fa-file-alt"></i>
        Content
      </a>
      <a href="/admin/{{#if model.slug}}{{model.slug}}{{else}}modelexample{{/if}}/gallery" class="nav-link {{#if (eq currentPage 'model-gallery')}}active{{/if}}">
        <i class="fas fa-images"></i>
        Gallery
      </a>
    </div>

    <!-- Insights -->
    <div class="nav-section">
      <div class="nav-section-title">Insights</div>
      <a href="#" class="nav-link">
        <i class="fas fa-chart-bar"></i>
        Analytics
        <span class="badge bg-secondary">Soon</span>
      </a>
    </div>

    <!-- Account -->
    <div class="nav-section">
      <div class="nav-section-title">Account</div>
      <a href="#" class="nav-link">
        <i class="fas fa-user-cog"></i>
        Profile & Settings
        <span class="badge bg-secondary">Soon</span>
      </a>
    </div>
  </nav>
</div>


