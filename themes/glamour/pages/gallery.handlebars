<!-- Gallery Hero Section -->
<section class="py-16 bg-gradient-to-r from-glamour-primary to-glamour-accent text-white" data-aos="fade-in">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center">
            <h1 class="text-4xl md:text-5xl font-bold mb-6 glamour-heading">
                Photo Gallery
            </h1>
            <p class="text-xl md:text-2xl text-pink-100 max-w-3xl mx-auto">
                {{#if content.galleryIntro}}
                    {{content.galleryIntro}}
                {{else}}
                    Discover my exclusive collection of professional photography
                {{/if}}
            </p>
        </div>
    </div>
</section>

<!-- Phase 5: Advanced Gallery Sections -->
<section class="py-16 bg-gradient-to-br from-pink-50 to-purple-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        {{#if (hasGalleries modelSlug)}}
            <!-- Render all gallery sections -->
            <div class="galleries-wrapper" data-aos="fade-up">
                {{{renderGalleries modelSlug}}}
            </div>
        {{else}}
            <!-- Empty State -->
            <div class="glamour-card p-12 text-center max-w-2xl mx-auto" data-aos="zoom-in">
                <div class="w-24 h-24 mx-auto mb-6 bg-glamour-primary rounded-full flex items-center justify-center">
                    <i class="fas fa-camera text-3xl text-white"></i>
                </div>
                <h3 class="text-2xl font-bold mb-4 glamour-heading">Gallery Coming Soon</h3>
                <p class="text-gray-600 mb-6">Professional photos will be available here shortly. Check back soon!</p>
                <a href="/{{modelSlug}}/contact" class="glamour-btn">
                    Get in Touch
                </a>
            </div>
        {{/if}}
    </div>
</section>

<!-- Glamour Theme Gallery Styling -->
<style>
.model-galleries .gallery-section {
    margin-bottom: 4rem;
}

.model-galleries .gallery-title {
    font-size: 2rem;
    font-weight: 700;
    background: linear-gradient(135deg, #e91e63, #9c27b0);
    background-clip: text;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-bottom: 2rem;
    text-align: center;
}

.model-galleries .grid-container {
    border-radius: 12px;
    overflow: hidden;
}

.model-galleries .grid-item {
    border-radius: 8px;
    overflow: hidden;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.model-galleries .grid-item:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(233, 30, 99, 0.2);
}

.model-galleries .grid-image-wrapper img {
    border-radius: 8px;
    transition: transform 0.3s ease;
}

.model-galleries .grid-image-wrapper:hover img {
    transform: scale(1.05);
}

.model-galleries .masonry-item {
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.model-galleries .masonry-item:hover {
    transform: translateY(-3px);
    box-shadow: 0 15px 30px rgba(233, 30, 99, 0.15);
}

.model-galleries .carousel-inner {
    box-shadow: 0 20px 40px rgba(0,0,0,0.1);
}

.model-galleries .lightbox-grid-item:hover {
    transform: scale(1.05);
    box-shadow: 0 10px 25px rgba(233, 30, 99, 0.3);
}

/* Glamour theme overrides for Bootstrap carousel */
.model-galleries .carousel-control-prev,
.model-galleries .carousel-control-next {
    background: linear-gradient(135deg, rgba(233, 30, 99, 0.8), rgba(156, 39, 176, 0.8));
    border-radius: 50%;
    width: 50px;
    height: 50px;
    top: 50%;
    transform: translateY(-50%);
}

.model-galleries .carousel-indicators [data-bs-target] {
    background-color: #e91e63;
    border-radius: 50%;
    width: 12px;
    height: 12px;
}
</style>

<!-- Call to Action -->
<section class="py-16 bg-gradient-to-r from-glamour-secondary to-glamour-primary text-white">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center" data-aos="fade-up">
        <h2 class="text-3xl md:text-4xl font-bold mb-6 glamour-heading">
            Like What You See?
        </h2>
        <p class="text-xl mb-8 text-pink-100 max-w-2xl mx-auto">
            These photos are just a preview. Let's discuss how we can create unforgettable moments together.
        </p>
        <div class="flex flex-col sm:flex-row justify-center gap-4">
            <a href="/{{modelSlug}}/contact" class="glamour-btn bg-white text-glamour-primary hover:bg-pink-50">
                <i class="fas fa-envelope mr-2"></i>
                Contact Me
            </a>
            <a href="/{{modelSlug}}/rates" class="glamour-btn bg-transparent border-2 border-white hover:bg-white hover:text-glamour-primary">
                <i class="fas fa-list mr-2"></i>
                View Services
            </a>
        </div>
    </div>
</section>

<!-- Phase 5 Gallery System includes its own lightbox and interactive features -->