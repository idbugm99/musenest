<!-- Rates Header -->
{{#if content.rates_header_visible}}
<section class="relative py-32 overflow-hidden" style="background: var(--glamour-gradient); color: white;">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <div class="mb-8">
            <i class="fas fa-crown text-6xl mb-6 text-white/80"></i>
        </div>
        <h1 class="text-5xl md:text-6xl font-bold mb-6" style="font-family: var(--font-display);">
            {{#if content.page_title}}{{content.page_title}}{{else}}Rates & Services{{/if}}
        </h1>
        {{#if content.page_subtitle}}
        <p class="text-xl text-white/90 max-w-3xl mx-auto">
            {{content.page_subtitle}}
        </p>
        {{else}}
        <p class="text-xl text-white/90 max-w-3xl mx-auto">
            Investment in time together. All arrangements are for companionship and social time only.
        </p>
        {{/if}}
    </div>
    
    <!-- Hero Divider -->
    {{#if content.hero_divider_visible}}
    <div class="text-center mt-8">
        {{#if (eq content.hero_divider_type "icon")}}
        <i class="{{#if content.hero_divider_icon}}{{content.hero_divider_icon}}{{else}}fas fa-gem{{/if}} text-3xl text-white/60"></i>
        {{else}}
        <div class="w-24 h-px mx-auto" style="background: linear-gradient(90deg, transparent, white, transparent);"></div>
        {{/if}}
    </div>
    {{/if}}
</section>
{{/if}}

<!-- Sticky Navigation -->
<div id="sticky-nav" class="sticky top-16 z-40 bg-white shadow-md transition-all duration-300" style="background: var(--theme-surface); margin-top: 64px;">
    <div class="max-w-7xl mx-auto">
        <nav class="flex flex-wrap justify-center gap-1 px-4 py-3">
            {{#if content.rates_table_visible}}
            <a href="#rates-section" data-section="rates-section" 
               class="nav-link px-4 py-2 rounded-lg text-sm font-medium transition-colors"
               style="color: var(--theme-text); hover:background: var(--theme-bg-alt);">
                {{#if content.nav_rates_label}}{{content.nav_rates_label}}{{else}}Rates{{/if}}
            </a>
            {{/if}}
            {{#if content.rates_extended_visible}}
            <a href="#extended-section" data-section="extended-section" 
               class="nav-link px-4 py-2 rounded-lg text-sm font-medium transition-colors"
               style="color: var(--theme-text); hover:background: var(--theme-bg-alt);">
                {{#if content.nav_extended_label}}{{content.nav_extended_label}}{{else}}Extended{{/if}}
            </a>
            {{/if}}
            {{#if content.rates_additional_visible}}
            <a href="#services-section" data-section="services-section" 
               class="nav-link px-4 py-2 rounded-lg text-sm font-medium transition-colors"
               style="color: var(--theme-text); hover:background: var(--theme-bg-alt);">
                {{#if content.nav_services_label}}{{content.nav_services_label}}{{else}}Services{{/if}}
            </a>
            {{/if}}
            {{#if content.terms_visible}}
            <a href="#policies-section" data-section="policies-section" 
               class="nav-link px-4 py-2 rounded-lg text-sm font-medium transition-colors"
               style="color: var(--theme-text); hover:background: var(--theme-bg-alt);">
                {{#if content.nav_policies_label}}{{content.nav_policies_label}}{{else}}Policies{{/if}}
            </a>
            {{/if}}
            {{#if content.rates_payment_visible}}
            <a href="#payment-section" data-section="payment-section" 
               class="nav-link px-4 py-2 rounded-lg text-sm font-medium transition-colors"
               style="color: var(--theme-text); hover:background: var(--theme-bg-alt);">
                {{#if content.nav_payment_label}}{{content.nav_payment_label}}{{else}}Payment{{/if}}
            </a>
            {{/if}}
            {{#if content.cta_visible}}
            <a href="#contact-section" data-section="contact-section" 
               class="nav-link px-4 py-2 rounded-lg text-sm font-medium transition-colors"
               style="color: var(--theme-text); hover:background: var(--theme-bg-alt);">
                {{#if content.nav_contact_label}}{{content.nav_contact_label}}{{else}}Contact{{/if}}
            </a>
            {{/if}}
        </nav>
    </div>
</div>

<!-- Service Rates Tables -->
{{#if content.rates_table_visible}}
<section id="rates-section" class="py-20" style="background-color: var(--theme-bg);">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
            <h2 class="text-4xl font-bold mb-6" style="color: var(--theme-text); font-family: var(--font-display);">
                {{#if content.table_title}}{{content.table_title}}{{else}}Service Rates{{/if}}
            </h2>
            {{#if content.table_description}}
            <p class="text-lg max-w-3xl mx-auto" style="color: var(--theme-text-subtle);">
                {{content.table_description}}
            </p>
            {{/if}}
        </div>

        <!-- Incall Services -->
        {{#if content.rates_incall_visible}}
        {{#if rates.incall.length}}
        <div class="mb-16">
            <h3 class="text-2xl font-bold mb-8 text-center" style="color: var(--theme-text); font-family: var(--font-display);">
                Incall Services
            </h3>
            <div class="glamour-card overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="w-full">
                        <thead style="background: var(--theme-bg-alt);">
                            <tr>
                                {{#if content.incall_show_service_name}}<th class="px-6 py-4 text-left font-semibold" style="color: var(--theme-text);">Service</th>{{/if}}
                                {{#if content.incall_show_duration}}<th class="px-6 py-4 text-left font-semibold" style="color: var(--theme-text);">Duration</th>{{/if}}
                                {{#if content.incall_show_price}}<th class="px-6 py-4 text-left font-semibold" style="color: var(--theme-text);">Investment</th>{{/if}}
                            </tr>
                        </thead>
                        <tbody>
                            {{#each rates.incall}}
                            <tr class="border-t" style="border-color: var(--theme-border);">
                                {{#if ../content.incall_show_service_name}}<td class="px-6 py-4 font-medium" style="color: var(--theme-text);">{{this.service_name}}</td>{{/if}}
                                {{#if ../content.incall_show_duration}}<td class="px-6 py-4" style="color: var(--theme-text-subtle);">{{this.duration}}</td>{{/if}}
                                {{#if ../content.incall_show_price}}<td class="px-6 py-4 font-semibold" style="color: var(--theme-primary);">{{this.price}}</td>{{/if}}
                            </tr>
                            {{#if this.description}}
                            <tr class="border-t" style="border-color: var(--theme-border-muted);">
                                <td colspan="3" class="px-6 py-2 text-sm" style="color: var(--theme-text-subtle);">{{this.description}}</td>
                            </tr>
                            {{/if}}
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        {{/if}}
        {{/if}}

        <!-- Outcall Services -->
        {{#if content.rates_outcall_visible}}
        {{#if rates.outcall.length}}
        <div class="mb-16">
            <h3 class="text-2xl font-bold mb-8 text-center" style="color: var(--theme-text); font-family: var(--font-display);">
                Outcall Services
            </h3>
            <div class="glamour-card overflow-hidden">
                <div class="overflow-x-auto">
                    <table class="w-full">
                        <thead style="background: var(--theme-bg-alt);">
                            <tr>
                                {{#if content.outcall_show_service_name}}<th class="px-6 py-4 text-left font-semibold" style="color: var(--theme-text);">Service</th>{{/if}}
                                {{#if content.outcall_show_duration}}<th class="px-6 py-4 text-left font-semibold" style="color: var(--theme-text);">Duration</th>{{/if}}
                                {{#if content.outcall_show_price}}<th class="px-6 py-4 text-left font-semibold" style="color: var(--theme-text);">Investment</th>{{/if}}
                            </tr>
                        </thead>
                        <tbody>
                            {{#each rates.outcall}}
                            <tr class="border-t" style="border-color: var(--theme-border);">
                                {{#if ../content.outcall_show_service_name}}<td class="px-6 py-4 font-medium" style="color: var(--theme-text);">{{this.service_name}}</td>{{/if}}
                                {{#if ../content.outcall_show_duration}}<td class="px-6 py-4" style="color: var(--theme-text-subtle);">{{this.duration}}</td>{{/if}}
                                {{#if ../content.outcall_show_price}}<td class="px-6 py-4 font-semibold" style="color: var(--theme-primary);">{{this.price}}</td>{{/if}}
                            </tr>
                            {{#if this.description}}
                            <tr class="border-t" style="border-color: var(--theme-border-muted);">
                                <td colspan="3" class="px-6 py-2 text-sm" style="color: var(--theme-text-subtle);">{{this.description}}</td>
                            </tr>
                            {{/if}}
                            {{/each}}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        {{/if}}
        {{/if}}
    </div>
</section>
{{/if}}

<!-- Extended Services -->
{{#if content.rates_extended_visible}}
{{#if rates.extended.length}}
<section id="extended-section" class="py-20" style="background-color: var(--theme-bg-alt);">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
            <h2 class="text-4xl font-bold mb-6" style="color: var(--theme-text); font-family: var(--font-display);">
                Extended Services
            </h2>
        </div>

        <div class="glamour-card overflow-hidden">
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead style="background: var(--theme-bg);">
                        <tr>
                            {{#if content.extended_show_service_name}}<th class="px-6 py-4 text-left font-semibold" style="color: var(--theme-text);">Service</th>{{/if}}
                            {{#if content.extended_show_duration}}<th class="px-6 py-4 text-left font-semibold" style="color: var(--theme-text);">Duration</th>{{/if}}
                            {{#if content.extended_show_price}}<th class="px-6 py-4 text-left font-semibold" style="color: var(--theme-text);">Investment</th>{{/if}}
                        </tr>
                    </thead>
                    <tbody>
                        {{#each rates.extended}}
                        <tr class="border-t" style="border-color: var(--theme-border);">
                            {{#if ../content.extended_show_service_name}}<td class="px-6 py-4 font-medium" style="color: var(--theme-text);">{{this.service_name}}</td>{{/if}}
                            {{#if ../content.extended_show_duration}}<td class="px-6 py-4" style="color: var(--theme-text-subtle);">{{this.duration}}</td>{{/if}}
                            {{#if ../content.extended_show_price}}<td class="px-6 py-4 font-semibold" style="color: var(--theme-primary);">{{this.price}}</td>{{/if}}
                        </tr>
                        {{#if this.description}}
                        <tr class="border-t" style="border-color: var(--theme-border-muted);">
                            <td colspan="3" class="px-6 py-2 text-sm" style="color: var(--theme-text-subtle);">{{this.description}}</td>
                        </tr>
                        {{/if}}
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</section>
{{/if}}
{{/if}}

<!-- Additional Services -->
{{#if content.rates_additional_visible}}
{{#if additionalServices.length}}
<section id="services-section" class="py-20" style="background-color: var(--theme-bg);">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
            <h2 class="text-4xl font-bold mb-6" style="color: var(--theme-text); font-family: var(--font-display);">
                Additional Services
            </h2>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {{#each additionalServices}}
            <div class="glamour-card p-8">
                <h3 class="text-xl font-semibold mb-4" style="color: var(--theme-text); font-family: var(--font-display);">
                    {{this.service_name}}
                </h3>
                <div class="text-2xl font-bold mb-4" style="color: var(--theme-primary);">
                    {{this.price}}
                </div>
                {{#if this.description}}
                <p style="color: var(--theme-text-subtle);">
                    {{this.description}}
                </p>
                {{/if}}
            </div>
            {{/each}}
        </div>
    </div>
</section>
{{/if}}
{{/if}}

<!-- Donations Section -->
{{#if content.donations_visible}}
{{#if donations.length}}
<section class="py-20" style="background-color: var(--theme-bg-alt);">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
            <h2 class="text-4xl font-bold mb-6" style="color: var(--theme-text); font-family: var(--font-display);">
                {{#if content.donation_title}}{{content.donation_title}}{{else}}Investment Information{{/if}}
            </h2>
            {{#if content.donation_description}}
            <p class="text-lg max-w-3xl mx-auto" style="color: var(--theme-text-subtle);">
                {{content.donation_description}}
            </p>
            {{/if}}
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {{#each donations}}
            <div class="glamour-card p-8 text-center">
                <h3 class="text-xl font-semibold mb-4" style="color: var(--theme-text);">
                    {{this.category}}
                </h3>
                <div class="text-3xl font-bold mb-4" style="color: var(--theme-primary);">
                    {{this.amount}}
                </div>
                {{#if this.description}}
                <p style="color: var(--theme-text-subtle);">
                    {{this.description}}
                </p>
                {{/if}}
            </div>
            {{/each}}
        </div>
    </div>
</section>
{{/if}}
{{/if}}

<!-- Terms & Policies -->
{{#if content.terms_visible}}
<section id="policies-section" class="py-20" style="background-color: var(--theme-bg);">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="glamour-card p-12">
            <h2 class="text-3xl font-bold mb-6 text-center" style="color: var(--theme-text); font-family: var(--font-display);">
                {{#if content.terms_title}}{{content.terms_title}}{{else}}Booking Information{{/if}}
            </h2>
            {{#if content.terms_content}}
            <div class="prose max-w-none" style="color: var(--theme-text-subtle);">
                {{content.terms_content}}
            </div>
            {{else}}
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <div>
                    <h3 class="text-xl font-semibold mb-4" style="color: var(--theme-primary);">
                        <i class="fas fa-shield-alt mr-2"></i>
                        Safety & Discretion
                    </h3>
                    <ul class="space-y-2" style="color: var(--theme-text-subtle);">
                        <li>• All arrangements are for companionship only</li>
                        <li>• Mutual respect and discretion expected</li>
                        <li>• Professional and confidential service</li>
                    </ul>
                </div>
                <div>
                    <h3 class="text-xl font-semibold mb-4" style="color: var(--theme-primary);">
                        <i class="fas fa-calendar-check mr-2"></i>
                        Booking Process
                    </h3>
                    <ul class="space-y-2" style="color: var(--theme-text-subtle);">
                        <li>• Advance booking recommended</li>
                        <li>• Deposit required for confirmation</li>
                        <li>• Flexible scheduling available</li>
                    </ul>
                </div>
            </div>
            {{/if}}
        </div>
    </div>
</section>
{{/if}}

<!-- Payment Information -->
{{#if content.rates_payment_visible}}
<section id="payment-section" class="py-20" style="background-color: var(--theme-bg-alt);">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
            <h2 class="text-4xl font-bold mb-6" style="color: var(--theme-text); font-family: var(--font-display);">
                Payment Information
            </h2>
        </div>
        
        <div class="glamour-card p-12">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div class="text-center">
                    <i class="fas fa-money-bill-wave text-4xl mb-4" style="color: var(--theme-primary);"></i>
                    <h3 class="text-lg font-semibold mb-2" style="color: var(--theme-text);">Cash</h3>
                    <p class="text-sm" style="color: var(--theme-text-subtle);">Preferred method</p>
                </div>
                <div class="text-center">
                    <i class="fas fa-university text-4xl mb-4" style="color: var(--theme-primary);"></i>
                    <h3 class="text-lg font-semibold mb-2" style="color: var(--theme-text);">Bank Transfer</h3>
                    <p class="text-sm" style="color: var(--theme-text-subtle);">Secure & discrete</p>
                </div>
                <div class="text-center">
                    <i class="fas fa-gift text-4xl mb-4" style="color: var(--theme-primary);"></i>
                    <h3 class="text-lg font-semibold mb-2" style="color: var(--theme-text);">Gifts</h3>
                    <p class="text-sm" style="color: var(--theme-text-subtle);">Always appreciated</p>
                </div>
            </div>
        </div>
    </div>
</section>
{{/if}}

<!-- CTA -->
{{#if content.cta_visible}}
<section id="contact-section" class="py-16" style="background-color: var(--theme-bg);">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <div class="glamour-card p-12">
            <h2 class="text-3xl font-bold mb-6" style="color: var(--theme-text); font-family: var(--font-display);">
                {{#if content.cta_title}}{{content.cta_title}}{{else}}Ready to Begin?{{/if}}
            </h2>
            {{#if content.cta_description}}
            <p class="text-lg mb-8" style="color: var(--theme-text-subtle);">
                {{content.cta_description}}
            </p>
            {{else}}
            <p class="text-lg mb-8" style="color: var(--theme-text-subtle);">
                Contact me to discuss your preferences and arrange our time together.
            </p>
            {{/if}}
            
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                {{#if content.cta_button_1_text}}
                <a href="/{{modelSlug}}/{{#if content.cta_button_1_link}}{{content.cta_button_1_link}}{{else}}contact{{/if}}{{previewParam}}" 
                   class="glamour-btn">
                    <i class="fas fa-envelope"></i>
                    {{content.cta_button_1_text}}
                </a>
                {{/if}}
                
                {{#if content.cta_button_2_text}}
                <a href="/{{modelSlug}}/{{#if content.cta_button_2_link}}{{content.cta_button_2_link}}{{else}}calendar{{/if}}{{previewParam}}" 
                   class="glamour-btn-outline">
                    <i class="fas fa-calendar-alt"></i>
                    {{content.cta_button_2_text}}
                </a>
                {{/if}}
                
                {{#unless content.cta_button_1_text}}{{#unless content.cta_button_2_text}}
                <!-- Default CTAs -->
                <a href="/{{modelSlug}}/contact{{previewParam}}" class="glamour-btn">
                    <i class="fas fa-envelope"></i>
                    Get in Touch
                </a>
                <a href="/{{modelSlug}}/calendar{{previewParam}}" class="glamour-btn-outline">
                    <i class="fas fa-calendar-alt"></i>
                    View Availability
                </a>
                {{/unless}}{{/unless}}
            </div>
        </div>
    </div>
</section>
{{/if}}

<script>
// Sticky navigation functionality
document.addEventListener('DOMContentLoaded', function() {
    const stickyNav = document.getElementById('sticky-nav');
    const navLinks = stickyNav.querySelectorAll('.nav-link');
    
    // Smooth scrolling for navigation links
    navLinks.forEach(link => {
        link.addEventListener('click', function(e) {
            e.preventDefault();
            const targetId = this.getAttribute('href').substring(1);
            const targetElement = document.getElementById(targetId);
            
            if (targetElement) {
                const offsetTop = targetElement.offsetTop - stickyNav.offsetHeight - 20;
                window.scrollTo({
                    top: offsetTop,
                    behavior: 'smooth'
                });
                
                // Update active state
                navLinks.forEach(l => l.classList.remove('active'));
                this.classList.add('active');
            }
        });
    });
    
    // Highlight active section on scroll
    window.addEventListener('scroll', function() {
        const scrollPosition = window.scrollY + stickyNav.offsetHeight + 100;
        
        navLinks.forEach(link => {
            const targetId = link.getAttribute('href').substring(1);
            const targetElement = document.getElementById(targetId);
            
            if (targetElement) {
                const offsetTop = targetElement.offsetTop;
                const offsetBottom = offsetTop + targetElement.offsetHeight;
                
                if (scrollPosition >= offsetTop && scrollPosition < offsetBottom) {
                    navLinks.forEach(l => l.classList.remove('active'));
                    link.classList.add('active');
                }
            }
        });
    });
});
</script>

<style>
.nav-link.active {
    background: var(--glamour-gradient) !important;
    color: white !important;
}

.nav-link:hover {
    transform: translateY(-1px);
}

/* Mobile responsive navigation */
@media (max-width: 768px) {
    #sticky-nav nav {
        overflow-x: auto;
        -webkit-overflow-scrolling: touch;
    }
    
    #sticky-nav nav::-webkit-scrollbar {
        display: none;
    }
}
</style>