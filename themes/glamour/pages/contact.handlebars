<!-- Professional Contact Hero Section -->
<section class="glamour-contact-hero" data-aos="fade-in">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center">
            {{#if contact_content.contact_header_visible}}
            <h1 class="glamour-hero-title" data-aos="fade-up" data-aos-delay="200">
                {{#if contact_content.page_title}}{{contact_content.page_title}}{{else}}Contact {{siteName}}{{/if}}
            </h1>
            <p class="glamour-hero-subtitle" data-aos="fade-up" data-aos-delay="400">
                {{#if contact_content.page_subtitle}}{{contact_content.page_subtitle}}{{else}}Let's connect and create something extraordinary together{{/if}}
            </p>
            {{/if}}
        </div>
    </div>
</section>

<!-- Professional Contact Layout -->
<section class="glamour-contact-main">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="glamour-contact-layout">
            <!-- Professional Info Panel -->
            {{#if contact_content.contact_direct_visible}}
            <div class="glamour-info-panel" data-aos="fade-right">
                <h2 class="glamour-info-title">
                    {{#if contact_content.direct_title}}{{contact_content.direct_title}}{{else}}Let's Connect{{/if}}
                </h2>
                
                <div class="glamour-info-list">
                    {{#if contactEmail}}
                    <div class="glamour-info-row">
                        <i class="fas fa-envelope glamour-info-icon"></i>
                        <div class="glamour-info-content">
                            <div class="glamour-info-label">{{#if contact_content.direct_email_label}}{{contact_content.direct_email_label}}{{else}}Email{{/if}}</div>
                            <a href="mailto:{{contactEmail}}" class="glamour-info-value glamour-info-link">
                                {{contactEmail}}
                            </a>
                        </div>
                    </div>
                    {{/if}}
                    
                    {{#if contactPhone}}
                    <div class="glamour-info-row">
                        <i class="fas fa-phone glamour-info-icon"></i>
                        <div class="glamour-info-content">
                            <div class="glamour-info-label">{{#if contact_content.direct_phone_label}}{{contact_content.direct_phone_label}}{{else}}Phone{{/if}}</div>
                            <a href="tel:{{contactPhone}}" class="glamour-info-value glamour-info-link">
                                {{contactPhone}}
                            </a>
                        </div>
                    </div>
                    {{/if}}
                    
                    {{#if location}}
                    <div class="glamour-info-row">
                        <i class="fas fa-map-marker-alt glamour-info-icon"></i>
                        <div class="glamour-info-content">
                            <div class="glamour-info-label">{{#if contact_content.location_title}}{{contact_content.location_title}}{{else}}Location{{/if}}</div>
                            <div class="glamour-info-value">{{location}}</div>
                        </div>
                    </div>
                    {{/if}}
                    
                    {{#if workingHours}}
                    <div class="glamour-info-row">
                        <i class="fas fa-clock glamour-info-icon"></i>
                        <div class="glamour-info-content">
                            <div class="glamour-info-label">{{#if contact_content.direct_response_label}}{{contact_content.direct_response_label}}{{else}}Availability{{/if}}</div>
                            <div class="glamour-info-value">{{workingHours}}</div>
                        </div>
                    </div>
                    {{/if}}
                </div>
                
                <!-- Social Links -->
                {{#if socialLinks}}
                <div class="mt-12">
                    <h3 class="text-xl font-semibold mb-6 glamour-heading" style="color: #F3EFE7;">Follow Me</h3>
                    <div class="flex space-x-4">
                        {{#each socialLinks}}
                        <a href="{{this.url}}" class="w-12 h-12 rounded-full flex items-center justify-center hover:shadow-lg transition-all duration-300 transform hover:scale-110 border-2" style="border-color: #E9C77A; color: #E9C77A;">
                            <i class="fab fa-{{this.icon}}"></i>
                        </a>
                        {{/each}}
                    </div>
                </div>
                {{/if}}
            </div>
            {{/if}}
            
            <!-- Contact Form -->
            {{#if contact_content.contact_form_visible}}
            <div class="glamour-contact-form" data-aos="fade-left">
                <div class="glamour-form-container">
                    <h2 class="glamour-form-title">
                        {{#if contact_content.form_title}}{{contact_content.form_title}}{{else}}Send a Message{{/if}}
                    </h2>
                    
                    <form id="contactForm" class="glamour-form">
                        <div class="glamour-form-row">
                            <div class="glamour-form-field">
                                <label for="firstName" class="glamour-form-label">
                                    {{#if contact_content.form_name_label}}{{contact_content.form_name_label}}{{else}}First Name{{/if}} *
                                </label>
                                <input type="text" id="firstName" name="firstName" required class="glamour-form-input">
                            </div>
                            <div class="glamour-form-field">
                                <label for="lastName" class="glamour-form-label">
                                    Last Name *
                                </label>
                                <input type="text" id="lastName" name="lastName" required class="glamour-form-input">
                            </div>
                        </div>
                        
                        <div class="glamour-form-field">
                            <label for="email" class="glamour-form-label">
                                {{#if contact_content.form_email_label}}{{contact_content.form_email_label}}{{else}}Email Address{{/if}} *
                            </label>
                            <input type="email" id="email" name="email" required class="glamour-form-input">
                        </div>
                        
                        <div class="glamour-form-field">
                            <label for="phone" class="glamour-form-label">
                                {{#if contact_content.form_phone_label}}{{contact_content.form_phone_label}}{{else}}Phone Number{{/if}}
                            </label>
                            <input type="tel" id="phone" name="phone" class="glamour-form-input">
                        </div>
                        
                        <div class="glamour-form-field">
                            <label for="subject" class="glamour-form-label">
                                Subject *
                            </label>
                            <select id="subject" name="subject" required class="glamour-form-input glamour-form-select">
                                <option value="">Select a subject</option>
                                <option value="general">General Inquiry</option>
                                <option value="booking">Booking Request</option>
                                <option value="rates">Services & Rates</option>
                                <option value="other">Other</option>
                            </select>
                        </div>
                        
                        <div class="glamour-form-field">
                            <label for="message" class="glamour-form-label">
                                {{#if contact_content.form_message_label}}{{contact_content.form_message_label}}{{else}}Message{{/if}} *
                            </label>
                            <textarea id="message" name="message" required class="glamour-form-textarea" placeholder="{{#if contact_content.form_message_placeholder}}{{contact_content.form_message_placeholder}}{{else}}Please include any specific requests or questions...{{/if}}"></textarea>
                        </div>
                        
                        <div class="glamour-form-field">
                            <button type="submit" class="glamour-form-submit">
                                <i class="fas fa-paper-plane"></i>
                                {{#if contact_content.form_button_text}}{{contact_content.form_button_text}}{{else}}Send Message{{/if}}
                            </button>
                        </div>
                    </form>
                </div>
            </div>
            {{/if}}
            
            <!-- Additional Sections Column -->
            <div class="glamour-additional-sections">
                <!-- Location Section -->
                {{#if contact_content.contact_location_visible}}
                <div class="glamour-info-panel mb-8" data-aos="fade-up" data-aos-delay="300">
                    <h3 class="glamour-info-title mb-4">
                        {{#if contact_content.location_title}}{{contact_content.location_title}}{{else}}Location & Services{{/if}}
                    </h3>
                    <div class="space-y-3">
                        {{#if contact_content.location_area_text}}
                        <p class="glamour-info-value">{{contact_content.location_area_text}}</p>
                        {{/if}}
                        {{#if contact_content.location_services_text}}
                        <p class="glamour-info-value">{{contact_content.location_services_text}}</p>
                        {{/if}}
                        {{#if contact_content.location_travel_text}}
                        <p class="glamour-info-value">{{contact_content.location_travel_text}}</p>
                        {{/if}}
                    </div>
                </div>
                {{/if}}
                
                <!-- Privacy Notice Section -->
                {{#if contact_content.contact_privacy_visible}}
                <div class="glamour-privacy-panel" data-aos="fade-up" data-aos-delay="400">
                    <h3 class="glamour-privacy-title">
                        {{#if contact_content.privacy_title}}{{contact_content.privacy_title}}{{else}}Privacy & Discretion{{/if}}
                    </h3>
                    <p class="glamour-privacy-text">
                        {{#if contact_content.privacy_text}}{{contact_content.privacy_text}}{{else}}All communications are handled with the utmost discretion and confidentiality. Your privacy is my priority, and I expect the same level of respect and professionalism in return.{{/if}}
                    </p>
                </div>
                {{/if}}
            </div>
        </div>
    </div>
</section>

<!-- What to Expect Section -->
{{#if contact_content.contact_guidelines_visible}}
<section class="glamour-expectations-section">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16" data-aos="fade-up">
            <h2 class="glamour-expectations-title">
                {{#if contact_content.guidelines_title}}{{contact_content.guidelines_title}}{{else}}What to Expect{{/if}}
            </h2>
        </div>
        
        <div class="glamour-expectations-grid">
            {{#if contact_content.guideline_1}}
            <div class="glamour-expectations-card" data-aos="fade-up" data-aos-delay="0">
                <div class="glamour-expectations-icon">
                    <i class="fas fa-reply"></i>
                </div>
                <h3 class="glamour-expectations-card-title">{{#if contact_content.direct_response_label}}{{contact_content.direct_response_label}}{{else}}Swift Response{{/if}}</h3>
                <p class="glamour-expectations-card-text">{{contact_content.guideline_1}}</p>
            </div>
            {{/if}}
            
            {{#if contact_content.guideline_2}}
            <div class="glamour-expectations-card" data-aos="fade-up" data-aos-delay="100">
                <div class="glamour-expectations-icon">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <h3 class="glamour-expectations-card-title">{{#if contact_content.privacy_title}}{{contact_content.privacy_title}}{{else}}Complete Discretion{{/if}}</h3>
                <p class="glamour-expectations-card-text">{{contact_content.guideline_2}}</p>
            </div>
            {{/if}}
            
            {{#if contact_content.guideline_3}}
            <div class="glamour-expectations-card" data-aos="fade-up" data-aos-delay="200">
                <div class="glamour-expectations-icon">
                    <i class="fas fa-gem"></i>
                </div>
                <h3 class="glamour-expectations-card-title">Professional Service</h3>
                <p class="glamour-expectations-card-text">{{contact_content.guideline_3}}</p>
            </div>
            {{/if}}
        </div>
    </div>
</section>
{{/if}}

<!-- Professional Contact Styles -->
<style>
/* Contact Professional Variables */
:root {
    --glamour-primary: #E9C77A;
    --glamour-primary-hover: #D9B66A;
    --glamour-card-bg: #1E1730;
    --glamour-input-bg: #211A31;
    --glamour-text-primary: #F3EFE7;
    --glamour-text-secondary: rgba(243, 239, 231, 0.8);
    --glamour-border: rgba(255, 255, 255, 0.08);
    --glamour-border-focus: #E9C77A;
    --glamour-focus-shadow: rgba(233, 199, 122, 0.25);
}

/* Professional Contact Hero */
.glamour-contact-hero {
    position: relative;
    min-height: 60vh;
    background: linear-gradient(160deg, #A61147 0%, #7E1B5C 45%, #2B203F 100%);
    display: flex;
    align-items: center;
    overflow: hidden;
    color: var(--glamour-text-primary);
}

.glamour-hero-title {
    font-size: 48px;
    line-height: 56px;
    font-weight: 800;
    color: var(--glamour-text-primary);
    text-align: center;
    margin-bottom: 1.5rem;
    background: linear-gradient(45deg, var(--glamour-text-primary) 30%, var(--glamour-primary) 70%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    text-shadow: 0 0 50px rgba(255, 255, 255, 0.18);
}

.glamour-hero-subtitle {
    font-size: 18px;
    line-height: 28px;
    color: rgba(245, 242, 234, 0.9);
    text-align: center;
    font-weight: 400;
    max-width: 600px;
    margin: 0 auto;
}

/* Professional Contact Layout */
.glamour-contact-main {
    background: linear-gradient(180deg, #0f0a15 0%, #1a0d1e 100%);
    padding: 4rem 0;
}

.glamour-contact-layout {
    display: grid;
    grid-template-columns: 33% 67%;
    gap: 4rem;
    align-items: start;
}

.glamour-additional-sections {
    grid-column: 1 / -1;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    margin-top: 3rem;
}

/* Professional Info Panel */
.glamour-info-panel {
    background: var(--glamour-card-bg);
    border-radius: 16px;
    padding: 2.5rem;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15);
    border: 1px solid var(--glamour-border);
    height: fit-content;
}

.glamour-info-title {
    font-size: 24px;
    line-height: 32px;
    font-weight: 600;
    color: var(--glamour-text-primary);
    margin-bottom: 2rem;
    letter-spacing: -0.01em;
}

.glamour-info-list {
    display: flex;
    flex-direction: column;
    gap: 0;
}

.glamour-info-row {
    display: flex;
    align-items: center;
    gap: 1rem;
    height: 56px;
    padding: 0 0.5rem;
    transition: all 0.2s ease;
}

.glamour-info-row:hover {
    background: rgba(255, 255, 255, 0.02);
    border-radius: 8px;
}

.glamour-info-icon {
    width: 24px;
    height: 24px;
    color: var(--glamour-primary);
    font-size: 20px;
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
}

.glamour-info-content {
    display: flex;
    flex-direction: column;
    gap: 2px;
    flex: 1;
}

.glamour-info-label {
    font-size: 12px;
    line-height: 16px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--glamour-text-secondary);
    font-weight: 500;
}

.glamour-info-value {
    font-size: 16px;
    line-height: 24px;
    color: var(--glamour-text-primary);
    font-weight: 500;
}

.glamour-info-link {
    color: var(--glamour-text-primary);
    text-decoration: none;
    transition: color 0.2s ease;
}

.glamour-info-link:hover {
    color: var(--glamour-primary);
    text-decoration: underline;
}

/* Professional Contact Form */
.glamour-contact-form {
    flex: 1;
}

.glamour-form-container {
    background: var(--glamour-card-bg);
    border-radius: 16px;
    padding: 2.5rem;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15);
    border: 1px solid var(--glamour-border);
}

.glamour-form-title {
    font-size: 24px;
    line-height: 32px;
    font-weight: 600;
    color: var(--glamour-text-primary);
    margin-bottom: 2rem;
    letter-spacing: -0.01em;
}

.glamour-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
}

.glamour-form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
}

.glamour-form-field {
    display: flex;
    flex-direction: column;
}

.glamour-form-label {
    font-size: 14px;
    line-height: 20px;
    font-weight: 500;
    color: var(--glamour-text-primary);
    margin-bottom: 8px;
}

.glamour-form-input,
.glamour-form-select,
.glamour-form-textarea {
    height: 44px;
    border-radius: 12px;
    background: var(--glamour-input-bg);
    border: 1px solid var(--glamour-border);
    color: var(--glamour-text-primary);
    padding: 0 16px;
    font-size: 16px;
    line-height: 24px;
    transition: all 0.2s ease;
    font-family: inherit;
}

.glamour-form-input:focus,
.glamour-form-select:focus,
.glamour-form-textarea:focus {
    outline: none;
    border-color: var(--glamour-border-focus);
    box-shadow: 0 0 0 3px var(--glamour-focus-shadow);
}

.glamour-form-textarea {
    height: auto;
    min-height: 160px;
    padding: 12px 16px;
    resize: vertical;
    line-height: 1.5;
}

.glamour-form-select {
    background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%23E9C77A' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpolyline points='6,9 12,15 18,9'%3e%3c/polyline%3e%3c/svg%3e");
    background-repeat: no-repeat;
    background-position: right 12px center;
    background-size: 16px;
    padding-right: 40px;
    cursor: pointer;
}

.glamour-form-submit {
    height: 44px;
    border-radius: 22px;
    background: var(--glamour-primary);
    color: #1a0d2e;
    border: none;
    padding: 0 24px;
    font-size: 16px;
    line-height: 1;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.2s ease;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    margin-top: 0.5rem;
}

.glamour-form-submit:hover {
    background: var(--glamour-primary-hover);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(233, 199, 122, 0.3);
}

/* Privacy Panel Styles */
.glamour-privacy-panel {
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
    border-radius: 16px;
    padding: 2rem;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
    border: 1px solid rgba(255, 255, 255, 0.1);
    position: relative;
    overflow: hidden;
}

.glamour-privacy-panel::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 3px;
    background: linear-gradient(90deg, var(--glamour-primary) 0%, #D9B66A 100%);
}

.glamour-privacy-title {
    font-size: 20px;
    line-height: 28px;
    font-weight: 600;
    color: var(--glamour-text-primary);
    margin-bottom: 1rem;
    letter-spacing: -0.01em;
}

.glamour-privacy-text {
    font-size: 14px;
    line-height: 22px;
    color: var(--glamour-text-secondary);
    margin: 0;
}

/* Professional Expectations Section */
.glamour-expectations-section {
    background: #0f0a15;
    padding: 4rem 0;
}

.glamour-expectations-title {
    font-size: 32px;
    line-height: 40px;
    font-weight: 600;
    color: var(--glamour-text-primary);
    text-align: center;
    margin-bottom: 0;
    letter-spacing: -0.01em;
}

.glamour-expectations-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
}

.glamour-expectations-card {
    background: var(--glamour-card-bg);
    border-radius: 16px;
    padding: 2rem;
    text-align: center;
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.15);
    border: 1px solid var(--glamour-border);
    transition: all 0.3s ease;
    display: flex;
    flex-direction: column;
    align-items: center;
    min-height: 260px;
    justify-content: center;
}

.glamour-expectations-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 16px 48px rgba(0, 0, 0, 0.25);
}

.glamour-expectations-icon {
    width: 56px;
    height: 56px;
    background: var(--glamour-primary);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    box-shadow: 0 4px 16px rgba(233, 199, 122, 0.2);
}

.glamour-expectations-icon i {
    font-size: 28px;
    color: #1a0d2e;
}

.glamour-expectations-card-title {
    font-size: 18px;
    line-height: 24px;
    font-weight: 600;
    color: var(--glamour-text-primary);
    margin-bottom: 1rem;
    letter-spacing: -0.01em;
}

.glamour-expectations-card-text {
    font-size: 14px;
    line-height: 22px;
    color: var(--glamour-text-secondary);
    text-align: center;
    margin: 0;
}

/* Responsive Design */
@media (max-width: 768px) {
    .glamour-contact-layout {
        grid-template-columns: 1fr;
        gap: 2rem;
    }
    
    .glamour-info-panel {
        order: 1;
    }
    
    .glamour-contact-form {
        order: 2;
    }
    
    .glamour-additional-sections {
        order: 3;
        grid-template-columns: 1fr;
        gap: 1.5rem;
        margin-top: 2rem;
    }
    
    .glamour-form-row {
        grid-template-columns: 1fr;
        gap: 1rem;
    }
    
    .glamour-expectations-grid {
        grid-template-columns: 1fr;
        gap: 1.5rem;
    }
    
    .glamour-hero-title {
        font-size: 36px;
        line-height: 42px;
    }
}

@media (max-width: 640px) {
    .glamour-info-panel,
    .glamour-form-container {
        padding: 2rem;
    }
    
    .glamour-expectations-card {
        padding: 1.5rem;
        min-height: 220px;
    }
}
</style>

<script>
document.getElementById('contactForm').addEventListener('submit', function(e) {
    e.preventDefault();
    
    // Get form data
    const formData = new FormData(this);
    const data = Object.fromEntries(formData.entries());
    
    // Here you would typically send the data to your backend
    console.log('Contact form submitted:', data);
    
    // Show success message (replace with actual implementation)
    alert('Thank you for your message! I will get back to you soon.');
    this.reset();
});
</script>