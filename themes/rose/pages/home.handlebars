<!-- Load Rose Theme Enhancements -->
<link rel="stylesheet" href="/themes/rose/assets/rose-enhancements.css">
<script src="/themes/rose/assets/rose-interactions.js"></script>

<!-- Enhanced Hero Section with Rose Garden -->
{{#if content.hero_section_visible}}
<section class="relative {{#if content.heroBackgroundImageUrl}}bg-cover bg-center bg-fixed{{else}}theme-accent-bg theme-text-inverted{{/if}} rose-garden-bg py-20 overflow-hidden" 
         {{#if content.heroBackgroundImageUrl}}style="background-image: url('{{content.heroBackgroundImageUrl}}')"{{/if}}>
    <!-- Sprite-based subtle icon row -->
    <!-- Removed auto sprite row -->
    
    {{#if content.heroBackgroundImageUrl}}
    <!-- Dynamic overlay for better text contrast (DB-driven) -->
    <div class="absolute inset-0 bg-black" style="opacity: {{#if content.hero_background_opacity}}{{content.hero_background_opacity}}{{else}}{{#if hero_background_opacity}}{{hero_background_opacity}}{{else}}0.6{{/if}}{{/if}};"></div>
    {{/if}}
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 {{#if content.heroBackgroundImageUrl}}relative z-10{{/if}}">
        <div class="text-center">
            <!-- Rose Section Header -->
            <div class="rose-section-header mb-8">
                <span class="header-text text-5xl font-bold leading-tight {{#unless content.heroBackgroundImageUrl}}theme-text-inverted{{/unless}}">
                    {{#if content.hero_title}}{{content.hero_title}}{{else}}{{siteName}}{{/if}}
                </span>
            </div>
            <p class="text-lg mb-8 max-w-3xl mx-auto leading-relaxed {{#if content.heroBackgroundImageUrl}}text-white opacity-80{{else}}theme-text-inverted opacity-90{{/if}}">
                {{#if content.hero_subtitle}}{{content.hero_subtitle}}{{else}}{{#if content.tagline}}{{content.tagline}}{{else}}Elegance and sophistication with discretion{{/if}}{{/if}}
            </p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <!-- Enhanced Primary Button -->
                {{#if content.hero_button_1_text}}
                    {{#if (eq content.hero_button_1_link 'calendar')}}
                        <a href="/{{modelSlug}}/calendar{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}bg-white text-gray-900 hover:bg-gray-100{{else}}theme-bg-tertiary theme-text-primary hover:opacity-90{{/if}} rose-button px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12 shadow-lg">{{content.hero_button_1_text}}</a>
                    {{else if (eq content.hero_button_1_link 'contact')}}
                        <a href="/{{modelSlug}}/contact{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}bg-white text-gray-900 hover:bg-gray-100{{else}}theme-bg-tertiary theme-text-primary hover:opacity-90{{/if}} rose-button px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12 shadow-lg">{{content.hero_button_1_text}}</a>
                    {{else if (eq content.hero_button_1_link 'about')}}
                        <a href="/{{modelSlug}}/about{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}bg-white text-gray-900 hover:bg-gray-100{{else}}theme-bg-tertiary theme-text-primary hover:opacity-90{{/if}} rose-button px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12 shadow-lg">{{content.hero_button_1_text}}</a>
                    {{else if (eq content.hero_button_1_link 'rates')}}
                        <a href="/{{modelSlug}}/rates{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}bg-white text-gray-900 hover:bg-gray-100{{else}}theme-bg-tertiary theme-text-primary hover:opacity-90{{/if}} rose-button px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12 shadow-lg">{{content.hero_button_1_text}}</a>
                    {{else if (eq content.hero_button_1_link 'gallery')}}
                        <a href="/{{modelSlug}}/gallery{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}bg-white text-gray-900 hover:bg-gray-100{{else}}theme-bg-tertiary theme-text-primary hover:opacity-90{{/if}} rose-button px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12 shadow-lg">{{content.hero_button_1_text}}</a>
                    {{else}}
                        <a href="/{{modelSlug}}/calendar{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}theme-accent-bg text-white hover:opacity-90{{else}}theme-bg-tertiary theme-text-primary hover:opacity-90{{/if}} px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.hero_button_1_text}}</a>
                    {{/if}}
                {{else}}
                    <a href="/{{modelSlug}}/calendar{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}bg-white text-gray-900 hover:bg-gray-100{{else}}theme-bg-tertiary theme-text-primary hover:opacity-90{{/if}} rose-button px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12 shadow-lg">View Calendar</a>
                {{/if}}
                
                <!-- Secondary Outline Button -->
                {{#if content.hero_button_2_text}}
                    {{#if (eq content.hero_button_2_link 'calendar')}}
                        <a href="/{{modelSlug}}/calendar{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}border-2 border-white text-white hover:bg-white hover:text-gray-900{{else}}theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted{{/if}} rose-button px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.hero_button_2_text}}</a>
                    {{else if (eq content.hero_button_2_link 'contact')}}
                        <a href="/{{modelSlug}}/contact{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}border-2 border-white text-white hover:bg-white hover:text-gray-900{{else}}theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted{{/if}} rose-button px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.hero_button_2_text}}</a>
                    {{else if (eq content.hero_button_2_link 'about')}}
                        <a href="/{{modelSlug}}/about{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}border-2 border-white text-white hover:bg-white hover:text-gray-900{{else}}theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted{{/if}} rose-button px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.hero_button_2_text}}</a>
                    {{else if (eq content.hero_button_2_link 'rates')}}
                        <a href="/{{modelSlug}}/rates{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}border-2 border-white text-white hover:bg-white hover:text-gray-900{{else}}theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted{{/if}} rose-button px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.hero_button_2_text}}</a>
                    {{else if (eq content.hero_button_2_link 'gallery')}}
                        <a href="/{{modelSlug}}/gallery{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}border-2 border-white text-white hover:bg-white hover:text-gray-900{{else}}theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted{{/if}} rose-button px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.hero_button_2_text}}</a>
                    {{else}}
                        <a href="/{{modelSlug}}/contact{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}border-2 border-white text-white hover:bg-white hover:text-gray-900{{else}}theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted{{/if}} px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.hero_button_2_text}}</a>
                    {{/if}}
                {{else}}
                    <a href="/{{modelSlug}}/contact{{previewParam}}" class="{{#if content.heroBackgroundImageUrl}}border-2 border-white text-white hover:bg-white hover:text-gray-900{{else}}theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted{{/if}} rose-button px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">Contact Me</a>
                {{/if}}
            </div>
        </div>
    </div>
    
    <!-- Decorative roses removed in favor of sprite icons -->
</section>
{{/if}}

<!-- Enhanced About Preview -->
{{#if content.about_section_visible}}
<section class="py-16 theme-section-alt rose-garden-bg relative overflow-hidden">
    <!-- Sprite divider on CTA band -->
    <!-- Removed CTA sprite divider -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        {{#if content.portrait_section_visible}}
        <div class="grid lg:grid-cols-2 gap-12 items-center">
            <div class="max-w-xl">
                <!-- Rose Section Header -->
                <div class="rose-section-header text-left mb-6">
                    <span class="header-text text-3xl font-bold theme-header">
                        {{#if content.about_title}}{{content.about_title}}{{else}}About {{siteName}}{{/if}}
                    </span>
                </div>
                {{#if content.about_paragraph_1}}
                <p class="text-base leading-relaxed mb-6 theme-muted-text">{{content.about_paragraph_1}}</p>
                {{/if}}
                {{#if content.about_paragraph_2}}
                <p class="text-base leading-relaxed mb-6 theme-muted-text">{{content.about_paragraph_2}}</p>
                {{/if}}
                {{#unless content.about_paragraph_1}}
                <p class="text-base leading-relaxed mb-6 theme-muted-text">Welcome to my world of elegance and sophistication. I am a professional companion who values discretion, intelligence, and genuine connection.</p>
                <p class="text-base leading-relaxed mb-6 theme-muted-text">With years of experience in providing exceptional companionship services, I pride myself on creating memorable experiences tailored to each client's unique needs.</p>
                {{/unless}}
                {{#if content.about_link_text}}
                    {{#eq content.about_link_destination 'about'}}
                        <a href="/{{modelSlug}}/about{{previewParam}}" class="rose-button theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.about_link_text}}</a>
                    {{/eq}}
                    {{#eq content.about_link_destination 'contact'}}
                        <a href="/{{modelSlug}}/contact{{previewParam}}" class="rose-button theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.about_link_text}}</a>
                    {{/eq}}
                    {{#eq content.about_link_destination 'gallery'}}
                        <a href="/{{modelSlug}}/gallery{{previewParam}}" class="rose-button theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.about_link_text}}</a>
                    {{/eq}}
                    {{#eq content.about_link_destination 'rates'}}
                        <a href="/{{modelSlug}}/rates{{previewParam}}" class="rose-button theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.about_link_text}}</a>
                    {{/eq}}
                    {{#eq content.about_link_destination 'calendar'}}
                        <a href="/{{modelSlug}}/calendar{{previewParam}}" class="rose-button theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.about_link_text}}</a>
                    {{/eq}}
                {{else}}
                    <a href="/{{modelSlug}}/about{{previewParam}}" class="rose-button theme-accent-border border-2 theme-accent hover:theme-accent-bg hover:theme-text-inverted px-6 py-3 rounded-full font-semibold transition-all duration-300 inline-flex items-center justify-center min-h-12">Learn More</a>
                {{/if}}
            </div>
            <div class="theme-bg-light rose-enhanced rounded-lg flex items-center justify-center overflow-hidden min-h-96 relative">
                {{#if content.portrait_image_url}}
                    <img src="{{content.portrait_image_url}}" 
                         alt="{{#if content.portrait_alt}}{{content.portrait_alt}}{{else}}Professional Portrait{{/if}}"
                         class="w-full h-full object-cover object-center">
                {{else}}
                    <p class="theme-text-subtle">{{#if content.portrait_alt}}{{content.portrait_alt}}{{else}}Professional portrait coming soon{{/if}}</p>
                {{/if}}
            </div>
        </div>
        {{else}}
        <div class="max-w-4xl mx-auto text-center">
            <h2 class="text-3xl font-bold mb-6 theme-header">
                {{#if content.about_title}}{{content.about_title}}{{else}}About {{siteName}}{{/if}}
            </h2>
            {{#if content.about_paragraph_1}}
            <p class="text-lg mb-6 theme-muted-text">{{content.about_paragraph_1}}</p>
            {{/if}}
            {{#if content.about_paragraph_2}}
            <p class="text-lg mb-6 theme-muted-text">{{content.about_paragraph_2}}</p>
            {{/if}}
            {{#unless content.about_paragraph_1}}
            <p class="text-lg mb-6 theme-muted-text">Welcome to my world of elegance and sophistication. I am a professional companion who values discretion, intelligence, and genuine connection.</p>
            <p class="text-lg mb-6 theme-muted-text">With years of experience in providing exceptional companionship services, I pride myself on creating memorable experiences tailored to each client's unique needs.</p>
            {{/unless}}
            {{#if content.about_link_text}}
                {{#eq content.about_link_destination 'about'}}
                    <a href="/{{modelSlug}}/about{{previewParam}}" class="theme-accent font-semibold hover:opacity-80 transition-opacity">{{content.about_link_text}}</a>
                {{/eq}}
                {{#eq content.about_link_destination 'contact'}}
                    <a href="/{{modelSlug}}/contact{{previewParam}}" class="theme-accent font-semibold hover:opacity-80 transition-opacity">{{content.about_link_text}}</a>
                {{/eq}}
                {{#eq content.about_link_destination 'gallery'}}
                    <a href="/{{modelSlug}}/gallery{{previewParam}}" class="theme-accent font-semibold hover:opacity-80 transition-opacity">{{content.about_link_text}}</a>
                {{/eq}}
                {{#eq content.about_link_destination 'rates'}}
                    <a href="/{{modelSlug}}/rates{{previewParam}}" class="theme-accent font-semibold hover:opacity-80 transition-opacity">{{content.about_link_text}}</a>
                {{/eq}}
                {{#eq content.about_link_destination 'calendar'}}
                    <a href="/{{modelSlug}}/calendar{{previewParam}}" class="theme-accent font-semibold hover:opacity-80 transition-opacity">{{content.about_link_text}}</a>
                {{/eq}}
            {{else}}
                <a href="/{{modelSlug}}/about{{previewParam}}" class="theme-accent font-semibold hover:opacity-80 transition-opacity">Learn More About Me →</a>
            {{/if}}
        </div>
        {{/if}}
    </div>
</section>
{{/if}}

<!-- Gallery Preview -->
{{#if content.gallery_section_visible}}
<section class="py-16 theme-section rose-garden-bg relative overflow-hidden">
    <!-- Sprite-based divider -->
    <!-- Removed gallery sprite divider -->
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
        <div class="rose-section-header text-center mb-12">
            <span class="header-text text-3xl font-bold theme-header">
                {{#if content.gallery_section_title}}{{content.gallery_section_title}}{{else}}🌹 Gallery{{/if}}
            </span>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-5 gap-4">
            {{#each galleryImages}}
            <div class="aspect-square overflow-hidden theme-bg-light rounded-xl">
                <img src="/uploads/{{../modelSlug}}/public/gallery/{{this.filename}}" 
                     alt="{{#if this.caption}}{{this.caption}}{{else}}Gallery image{{/if}}" 
                     class="w-full h-full object-cover hover:scale-105 transition-transform duration-300">
            </div>
            {{else}}
            {{#times 5}}
            <div class="theme-bg-light aspect-square flex items-center justify-center rounded-xl">
                <p class="theme-text-subtle text-sm">Photo</p>
            </div>
            {{/times}}
            {{/each}}
        </div>
        <div class="text-center mt-8">
            <a href="/{{modelSlug}}/gallery{{previewParam}}" class="rose-button theme-accent-bg theme-text-inverted px-6 py-3 rounded-full font-semibold hover:opacity-90 transition-all duration-300 inline-flex items-center justify-center min-h-12">
                {{#if content.gallery_button_text}}{{content.gallery_button_text}}{{else}}View Full Gallery{{/if}}
            </a>
        </div>
    </div>
</section>
{{/if}}

<!-- Location Availability -->
{{#if content.travel_section_visible}}
<section class="py-16 theme-section-alt">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-3xl font-bold text-center mb-12 theme-header">
            {{#if content.travel_section_title}}{{content.travel_section_title}}{{else}}Location Availability{{/if}}
        </h2>
        {{#if upcomingEvents}}
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
            {{#each upcomingEvents}}
            <div class="theme-card p-6 hover:shadow-lg transition-shadow">
                <div class="text-center mb-4">
                    <div class="inline-flex items-center justify-center theme-accent rounded-full mb-3 w-10 h-10 theme-bg-secondary">
                        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                        </svg>
                    </div>
                    <h3 class="text-xl font-bold mb-2 theme-text-primary">{{this.location}}</h3>
                    <div class="text-base font-medium mb-3 theme-text-muted">
                        {{#if this.dateRange}}
                            {{this.dateRange}}
                        {{else}}
                            {{formatDate this.date}}
                        {{/if}}
                    </div>
                </div>
                <div class="text-center">
                    <span class="text-white px-3 py-1 rounded-full text-sm font-bold theme-success bg-green-500">Available</span>
                    {{#if this.notes}}
                    <p class="text-xs leading-relaxed mt-2 theme-text-subtle">{{this.notes}}</p>
                    {{/if}}
                </div>
            </div>
            {{/each}}
        </div>
        <div class="text-center mt-8">
            <a href="/{{modelSlug}}/{{#if content.travel_cta_link}}{{content.travel_cta_link}}{{else}}calendar{{/if}}{{previewParam}}" class="theme-accent-bg theme-text-inverted px-6 py-3 rounded-full font-semibold hover:opacity-90 transition-all duration-300 inline-flex items-center justify-center min-h-12">
                {{#if content.travel_cta_text}}{{content.travel_cta_text}}{{else}}View Full Calendar{{/if}}
            </a>
        </div>
        {{else}}
        <div class="text-center">
            <div class="theme-card p-12 max-w-lg mx-auto">
                <div class="w-16 h-16 mx-auto mb-6 theme-accent-bg flex items-center justify-center rounded-xl">
                    <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                    </svg>
                </div>
                <h3 class="text-xl font-semibold mb-4 theme-text-primary">Check My Calendar</h3>
                <p class="mb-6 theme-text-muted">View available dates and book your appointment</p>
                <a href="/{{modelSlug}}/calendar{{previewParam}}" class="theme-accent-bg theme-text-inverted px-6 py-3 rounded-full font-semibold hover:opacity-90 transition-all duration-300 inline-flex items-center justify-center min-h-12">
                    View Calendar
                </a>
            </div>
        </div>
        {{/if}}
    </div>
</section>
{{/if}}

<!-- Testimonials -->
{{#if content.testimonials_section_visible}}
<section class="py-16 theme-section">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <h2 class="text-3xl font-bold text-center mb-12 theme-header">
            {{#if content.testimonials_section_title}}{{content.testimonials_section_title}}{{else}}What Clients Say{{/if}}
        </h2>
        <div class="grid md:grid-cols-3 gap-6 mb-6">
            {{#each testimonials}}
            {{#if (lt @index (or ../content.testimonials_display_count 3))}}
            <div class="theme-card p-6 shadow">
                <div class="flex mb-4 text-yellow-400">
                    {{#times this.rating}}
                    <svg class="fill-current w-4 h-4" viewBox="0 0 20 20">
                        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                    </svg>
                    {{/times}}
                </div>
                <p class="text-base leading-relaxed mb-4 theme-body-text">"{{this.text}}"</p>
                <p class="text-xs font-bold uppercase tracking-wide theme-text-subtle">{{this.name}}</p>
            </div>
            {{/if}}
            {{else}}
            <!-- Default testimonials if none provided -->
            {{#times (or content.testimonials_display_count 3)}}
            {{#if (eq this 1)}}
            <div class="theme-card p-6 shadow">
                <div class="flex mb-4 text-yellow-400">
                    {{#times 5}}
                    <svg class="fill-current w-4 h-4" viewBox="0 0 20 20">
                        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                    </svg>
                    {{/times}}
                </div>
                <p class="text-base leading-relaxed mb-4 theme-body-text">"She is absolutely wonderful. Professional, elegant, and truly caring. An unforgettable experience."</p>
                <p class="text-xs font-bold uppercase tracking-wide theme-text-subtle">MICHAEL D.</p>
            </div>
            {{else if (eq this 2)}}
            <div class="theme-card p-6 shadow">
                <div class="flex mb-4 text-yellow-400">
                    {{#times 5}}
                    <svg class="fill-current w-4 h-4" viewBox="0 0 20 20">
                        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                    </svg>
                    {{/times}}
                </div>
                <p class="text-base leading-relaxed mb-4 theme-body-text">"She is intelligent, sophisticated, and absolutely captivating. A truly remarkable companion."</p>
                <p class="text-xs font-bold uppercase tracking-wide theme-text-subtle">DAVID L.</p>
            </div>
            {{else if (eq this 3)}}
            <div class="theme-card p-6 shadow">
                <div class="flex mb-4 text-yellow-400">
                    {{#times 5}}
                    <svg class="fill-current w-4 h-4" viewBox="0 0 20 20">
                        <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"/>
                    </svg>
                    {{/times}}
                </div>
                <p class="text-base leading-relaxed mb-4 theme-body-text">"Exceptional service and genuine warmth. Highly recommended for discerning clients."</p>
                <p class="text-xs font-bold uppercase tracking-wide theme-text-subtle">ROBERT T.</p>
            </div>
            {{/if}}
            {{/times}}
            {{/each}}
        </div>
    </div>
</section>
{{/if}}

<!-- Bottom CTA Band -->
{{#if content.cta_section_visible}}
<section class="py-16 theme-accent-bg theme-text-inverted rose-garden-bg-inverted relative overflow-hidden">
    <!-- Floating Rose Petals -->
    <div class="rose-petals">
        <div class="rose-petal"></div>
        <div class="rose-petal"></div>
    </div>
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-3xl font-bold mb-6">
            {{#if content.cta_section_title}}{{content.cta_section_title}}{{else}}Ready to Meet?{{/if}}
        </h2>
        <p class="text-xl mb-8 opacity-90">
            {{#if content.cta_section_subtitle}}{{content.cta_section_subtitle}}{{else}}Experience elegance and sophistication with professional companionship services{{/if}}
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <!-- Primary Button (Calendar) -->
            {{#if content.cta_button_1_text}}
                {{#if (eq content.cta_button_1_link 'calendar')}}
                    <a href="/{{modelSlug}}/calendar{{previewParam}}" class="theme-bg-tertiary theme-text-primary px-6 py-3 rounded-full font-semibold hover:opacity-90 transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.cta_button_1_text}}</a>
                {{else if (eq content.cta_button_1_link 'contact')}}
                    <a href="/{{modelSlug}}/contact{{previewParam}}" class="theme-bg-tertiary theme-text-primary px-6 py-3 rounded-full font-semibold hover:opacity-90 transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.cta_button_1_text}}</a>
                {{else if (eq content.cta_button_1_link 'gallery')}}
                    <a href="/{{modelSlug}}/gallery{{previewParam}}" class="theme-bg-tertiary theme-text-primary px-6 py-3 rounded-full font-semibold hover:opacity-90 transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.cta_button_1_text}}</a>
                {{else if (eq content.cta_button_1_link 'rates')}}
                    <a href="/{{modelSlug}}/rates{{previewParam}}" class="theme-bg-tertiary theme-text-primary px-6 py-3 rounded-full font-semibold hover:opacity-90 transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.cta_button_1_text}}</a>
                {{else if (eq content.cta_button_1_link 'about')}}
                    <a href="/{{modelSlug}}/about{{previewParam}}" class="theme-bg-tertiary theme-text-primary px-6 py-3 rounded-full font-semibold hover:opacity-90 transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.cta_button_1_text}}</a>
                {{else}}
                    <a href="/{{modelSlug}}/calendar{{previewParam}}" class="theme-bg-tertiary theme-text-primary px-6 py-3 rounded-full font-semibold hover:opacity-90 transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.cta_button_1_text}}</a>
                {{/if}}
            {{else}}
                <a href="/{{modelSlug}}/calendar{{previewParam}}" class="theme-bg-tertiary theme-text-primary px-6 py-3 rounded-full font-semibold hover:opacity-90 transition-all duration-300 inline-flex items-center justify-center min-h-12">Calendar</a>
            {{/if}}
            
            <!-- Secondary Outline Button (Contact) -->
            {{#if content.cta_button_2_text}}
                {{#if (eq content.cta_button_2_link 'calendar')}}
                    <a href="/{{modelSlug}}/calendar{{previewParam}}" class="border-2 border-white text-white px-6 py-3 rounded-full font-semibold hover:bg-white hover:text-gray-900 transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.cta_button_2_text}}</a>
                {{else if (eq content.cta_button_2_link 'contact')}}
                    <a href="/{{modelSlug}}/contact{{previewParam}}" class="border-2 border-white text-white px-6 py-3 rounded-full font-semibold hover:bg-white hover:text-gray-900 transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.cta_button_2_text}}</a>
                {{else if (eq content.cta_button_2_link 'gallery')}}
                    <a href="/{{modelSlug}}/gallery{{previewParam}}" class="border-2 border-white text-white px-6 py-3 rounded-full font-semibold hover:bg-white hover:text-gray-900 transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.cta_button_2_text}}</a>
                {{else if (eq content.cta_button_2_link 'rates')}}
                    <a href="/{{modelSlug}}/rates{{previewParam}}" class="border-2 border-white text-white px-6 py-3 rounded-full font-semibold hover:bg-white hover:text-gray-900 transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.cta_button_2_text}}</a>
                {{else if (eq content.cta_button_2_link 'about')}}
                    <a href="/{{modelSlug}}/about{{previewParam}}" class="border-2 border-white text-white px-6 py-3 rounded-full font-semibold hover:bg-white hover:text-gray-900 transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.cta_button_2_text}}</a>
                {{else}}
                    <a href="/{{modelSlug}}/contact{{previewParam}}" class="border-2 border-white text-white px-6 py-3 rounded-full font-semibold hover:bg-white hover:text-gray-900 transition-all duration-300 inline-flex items-center justify-center min-h-12">{{content.cta_button_2_text}}</a>
                {{/if}}
            {{else}}
                <a href="/{{modelSlug}}/contact{{previewParam}}" class="border-2 border-white text-white px-6 py-3 rounded-full font-semibold hover:bg-white hover:text-gray-900 transition-all duration-300 inline-flex items-center justify-center min-h-12">Contact</a>
            {{/if}}
        </div>
    </div>
</section>
{{/if}}

<!-- Mobile Sticky CTA -->
<div id="mobile-sticky-cta" class="fixed bottom-0 left-0 right-0 theme-accent-bg theme-text-inverted p-4 z-50 hidden sm:hidden shadow-lg">
    <div class="max-w-sm mx-auto">
        <a href="/{{modelSlug}}/contact{{previewParam}}" class="block w-full theme-bg-tertiary theme-text-primary px-4 py-3 rounded-full font-semibold text-center hover:opacity-90 transition-all duration-300">
            Contact Me
        </a>
    </div>
</div>

<script>
// Mobile sticky CTA logic
document.addEventListener('DOMContentLoaded', function() {
    const hero = document.querySelector('[class*="theme-accent-bg"]');
    const stickyCTA = document.getElementById('mobile-sticky-cta');
    
    if (hero && stickyCTA && window.innerWidth < 640) {
        const observer = new IntersectionObserver(
            function(entries) {
                entries.forEach(function(entry) {
                    if (!entry.isIntersecting) {
                        stickyCTA.classList.remove('hidden');
                    } else {
                        stickyCTA.classList.add('hidden');
                    }
                });
            },
            { threshold: 0 }
        );
        
        observer.observe(hero);
    }
});
</script>

<style>
/* Enhanced Parallax Background Fix */
.bg-fixed {
    background-attachment: fixed !important;
    background-size: cover !important;
    background-position: center center !important;
    background-repeat: no-repeat !important;
}

/* Mobile parallax fallback */
@media (max-width: 768px) {
    .bg-fixed {
        background-attachment: scroll !important;
    }
}

/* iOS Safari parallax fallback */
@supports (-webkit-overflow-scrolling: touch) {
    .bg-fixed {
        background-attachment: scroll !important;
    }
}

/* Ensure minimum height for hero section */
section.bg-fixed {
    min-height: 70vh;
}

/* Force background image display with higher specificity */
section[style*="background-image"] {
    background-size: cover !important;
    background-position: center !important;
    background-repeat: no-repeat !important;
    background-attachment: fixed !important;
}

/* Debug: Show background color when no image */
section.bg-fixed:not([style*="background-image"]) {
    background-color: #dc2626 !important;
}
</style>